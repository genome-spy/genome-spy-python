"""Schema wrapper generation helpers for GenomeSpy Python.

This package intentionally mirrors Altair's ``tools/schemapi`` layout, but the
implementation starts small and GenomeSpy-specific. It gives us a local place
to evolve schema inspection, wrapper generation, and runtime helper copying
without editing generated package code by hand.
"""
