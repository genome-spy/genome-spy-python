# Devlog archive: 2026-07-01 to 2026-07-15

## 2026-07-15 - Added a TCGA LUAD cohort oncoprint example adapted from pyoncoprint

- Added the packaged dataset `tcga_luad_oncoprint` under
  `src/genome_spy/datasets/data/`, derived from the `tmp/pyoncoprint`
  notebook's TCGA LUAD tabular export and normalized into a docs-friendly JSON
  payload containing the sorted alteration matrix, top sample summary tracks,
  right-side gene summaries, and lower quantitative heatmaps.
- Registered the dataset in `src/genome_spy/datasets/__init__.py` and added
  `tests/test_datasets.py` coverage for the curated payload shape and key cohort
  counts so the new example is backed by a stable fixture.
- Added `docs/examples/luad_oncoprint.py`, a zoomable large-cohort oncoprint
  that recreates the notebook structure with shared sample-axis top tracks, a
  layered central mutation matrix, right-side per-gene summaries, and grouped
  bottom heatmaps.
- Validated with `UV_CACHE_DIR=.uv-cache uv run ruff check
  docs/examples/luad_oncoprint.py src/genome_spy/datasets/__init__.py
  tests/test_datasets.py`, `UV_CACHE_DIR=.uv-cache uv run pytest
  tests/test_datasets.py tests/test_docs_gallery.py -q`, and
  `UV_CACHE_DIR=.uv-cache uv run sphinx-build -E -b html -W --keep-going docs
  docs/_build/html`.

## 2026-07-15 - Reduced the remaining easy raw-dict examples with dedicated helpers and a datum constructor

- Audited the current docs examples for raw mapping usage rather than assuming
  the small gallery was representative. The concrete hotspots were narrower
  than expected: a few transform payloads (`coverage`, `flattenSequence`,
  `regexExtract`, and a bare `flatten`), two constant datum channels, one axis
  mapping, and one handled-tooltip mapping.
- Added dedicated chart helpers in `src/genome_spy/chart.py` for
  `transform_coverage(...)`, `transform_flatten_sequence(...)`, and
  `transform_regex_extract(...)`, and relaxed `transform_flatten(...)` so it
  can also emit the no-fields form used by the GFF3 example.
- Added a small Altair-style `gs.datum(...)` helper in
  `src/genome_spy/channels.py` and exported `HandledTooltip` at the public API
  layer so constant datum channels and handled tooltips no longer need raw
  dictionaries in the examples.
- Rewrote the affected examples (`bam_read_alignments`, `indexed_fasta_sequence`,
  `gff3_gene_annotations`, `clinvar_variants`, `needle_plot`,
  `rainfall_plot`, and `genome_tracks`) to use the new helpers or existing
  generated setters instead of raw mappings where the API now supports it.
- Added regression coverage in `tests/test_chart.py` for the new transform
  helpers, the public `datum(...)` helper, and the top-level `HandledTooltip`
  export.
- Validated with `UV_CACHE_DIR=.uv-cache uv run ruff check ...`,
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`,
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py
  tests/test_docs_gallery.py -q`, and
  `UV_CACHE_DIR=.uv-cache uv run sphinx-build -E -b html -W --keep-going docs
  docs/_build/html`.

## 2026-07-15 - Added a curated rainfall plot example and broadened the mutation-plot gallery bucket

- Added the packaged JSON dataset `tcga_brca_rainfall` under
  `src/genome_spy/datasets/data/`, derived from the single-sample
  `maftools::brca.maf.gz` example and reduced to the columns the docs example
  actually encodes: per-mutation locus, substitution class, inter-event
  distance, and compact kataegis-style change-point annotations.
- Added `docs/examples/rainfall_plot.py`, a concise locus-aware rainfall plot
  example that uses the packaged payload and mirrors the familiar maftools
  visualization shape without depending on `tmp/` or external preprocessing at
  docs-build time.
- Renamed the gallery category `"Lollipop and pathogenicity plots"` to
  `"Mutation position plots"` and moved the existing lollipop and ClinVar
  examples under that broader plot-type bucket so rainfall has a natural home.
- Updated dataset and gallery tests accordingly, then validated with
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_datasets.py
  tests/test_docs_gallery.py -q`,
  `UV_CACHE_DIR=.uv-cache uv run ruff check ...`,
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`, and
  `UV_CACHE_DIR=.uv-cache uv run sphinx-build -E -b html -W --keep-going docs
  docs/_build/html`.

## 2026-07-15 - Top-level generated-style `with_*` setters now work as a real chart-layer alternative

- Discovered that generated top-level schema setters such as `with_view(...)`,
  `with_config(...)`, and `with_scales(...)` already existed on chart and
  composition objects through generated schema inheritance, but they were not a
  strong alternative yet because repeated nested keyword updates replaced the
  previous value instead of merging it.
- Added handwritten `TopLevelSpec` overrides for `with_view(...)`,
  `with_config(...)`, and `with_scales(...)` that preserve `Self` return types
  and use the same merge-style semantics as the rest of the fluent chart API.
- Added regression tests in `tests/test_chart.py` for merged repeated calls and
  chart-type preservation, and updated `docs/api.md` plus
  `docs/getting-started.md` to mention these generated-style top-level setters
  explicitly.
- This gives us a concrete path to reduce reliance on `gs.config(...)` and
  `gs.view(...)` without needing a new generator pass immediately.
- Validated with `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py -q`,
  `UV_CACHE_DIR=.uv-cache uv run ruff check src/genome_spy/chart.py
  tests/test_chart.py docs/api.md docs/getting-started.md`, and
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`, plus
  `UV_CACHE_DIR=.uv-cache uv run sphinx-build -E -b html -W --keep-going docs
  docs/_build/html`.

## 2026-07-15 - Generator target list is now narrowed to config-family overlap

- Revisited the remaining handwritten surface from the generator perspective
  and concluded that most tiny helpers are no longer good codegen targets.
- Updated `plans/implementation_status.md` with a narrowed "Remaining
  Generator Targets" section: the clearest remaining codegen candidate is the
  top-level config compatibility layer (`config(...)`, `view(...)`,
  `view_config(...)`), while helpers such as `Locus(...)`, `value(...)`,
  `compare(...)`, `expr(...)`, `step(...)`, `parse(...)`, and
  `data_format(...)` now look settled as intentional manual ergonomics.
- Updated `plans/altair_alignment_plan.md` so the "Suggested Immediate Next
  Task" section reflects the same narrowed conclusion.
- Validated with `UV_CACHE_DIR=.uv-cache uv run ruff check
  plans/implementation_status.md plans/altair_alignment_plan.md`.

## 2026-07-15 - API docs now say explicitly that some remaining helper usage is intentional

- Tightened `docs/api.md` so it now states directly that the remaining gallery
  uses of `gs.scales(...)` and `gs.param(...)` are intentional where those
  helpers keep visualization code shorter and clearer.
- This matches the docs/example audit result: most remaining compatibility-
  helper usage is justified rather than drift away from the generated surface.
- Validated with `UV_CACHE_DIR=.uv-cache uv run ruff check docs/api.md
  README.md docs/getting-started.md` and `UV_CACHE_DIR=.uv-cache uv run
  sphinx-build -E -b html -W --keep-going docs docs/_build/html`.

## 2026-07-15 - Docs audit confirms remaining compatibility-helper usage is intentional

- Audited the docs and examples for remaining uses of compatibility helpers
  such as `gs.config(...)`, `gs.view_config(...)`, `gs.scales(...)`, and
  `gs.param(...)`.
- The remaining usage is mostly `gs.scales(...)` and `gs.param(...)`, which
  are still justified because they cover mapping-shaped or ergonomic cases
  rather than displaced generated config methods.
- Tightened `README.md` and `docs/getting-started.md` so they now state that
  generated `configure_* (...)` methods are preferred for top-level config,
  while `gs.scales(...)` and `gs.param(...)` remain useful where they keep the
  visualization code clearer.
- Validated with `UV_CACHE_DIR=.uv-cache uv run ruff check README.md
  docs/getting-started.md`, `UV_CACHE_DIR=.uv-cache uv run pytest
  tests/test_docs_gallery.py -q`, and `UV_CACHE_DIR=.uv-cache uv run
  sphinx-build -E -b html -W --keep-going docs docs/_build/html`.

## 2026-07-15 - Helper module now separates tiny constructors from compatibility wrappers

- Reshaped `src/genome_spy/helpers.py` so the module now states its two roles
  directly: tiny readability-first constructors (`expr(...)`, `title(...)`,
  `step(...)`, `parse(...)`, `data_format(...)`, `dynamic_opacity(...)`) and
  thin compatibility wrappers around generated config or mapping surfaces
  (`config(...)`, `view(...)`, `view_config(...)`, `scales(...)`, `param(...)`).
- Added small private normalization helpers so the compatibility wrappers now
  share one internal implementation path instead of each carrying bespoke local
  normalization logic.
- Updated `docs/api.md` so the API reference reflects the same distinction.
- Validated with `UV_CACHE_DIR=.uv-cache uv run ruff check
  src/genome_spy/helpers.py docs/api.md`,
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py -q`, and
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`.

## 2026-07-15 - Remaining handwritten helper and channel surface is now classified more explicitly

- Updated `plans/implementation_status.md` so the small helper constructors
  `dynamic_opacity(...)`, `parse(...)`, and `data_format(...)` are now treated
  as intentionally handwritten readability helpers rather than as unresolved
  transitional API debt.
- Kept `config(...)`, `view(...)`, `view_config(...)`, `scales(...)`, and
  `param(...)` in the transitional bucket, which better matches their current
  role as thin compatibility or mapping helpers around the generated schema
  surface.
- Added `__all__` and clearer role-oriented docstrings in
  `src/genome_spy/channels.py` so the module now states more explicitly that
  `Locus(...)`/`locus(...)` are intentional handwritten ergonomics while the
  base `Channel` class is mainly runtime support for generated wrappers.
- Validated with `UV_CACHE_DIR=.uv-cache uv run ruff check
  src/genome_spy/channels.py plans/implementation_status.md`,
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py -q`, and
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`.

## 2026-07-15 - Chart authoring normalization now lives in a dedicated private helper module

- Extracted the authoring-edge data/encoding/transform normalization helpers
  from `src/genome_spy/chart.py` into the new private module
  `src/genome_spy/_chart_authoring.py`.
- This moves record extraction, JSON-safe value conversion, field-type
  inference, channel normalization, default reversed `y`-scale behavior, and
  transform normalization behind a tighter boundary while keeping the logic
  close to the handwritten chart layer.
- `src/genome_spy/chart.py` now focuses more clearly on workflow API, shared
  `TopLevelSpec` builder plumbing, and generated-schema adaptation rather than
  carrying the full normalization block inline.
- Updated `plans/chart_layer_inventory.md` so the chart-layer architecture
  notes reflect the new module boundary.
- Validated with `UV_CACHE_DIR=.uv-cache uv run ruff check
  src/genome_spy/chart.py src/genome_spy/_chart_authoring.py
  tests/test_chart.py`, `UV_CACHE_DIR=.uv-cache uv run pytest
  tests/test_chart.py -q`, and `UV_CACHE_DIR=.uv-cache uv run mypy src/`.

## 2026-07-15 - Chart layer inventory now documents the remaining consolidation seam

- Added `plans/chart_layer_inventory.md` to split `src/genome_spy/chart.py`
  into explicit responsibility buckets: authoring-edge normalization, shared
  top-level plumbing, public workflow API, and generated-schema adaptation.
- Consolidated one real remaining duplication seam in `src/genome_spy/chart.py`
  by moving top-level data normalization and root-validation wrapping behind
  shared `TopLevelSpec` helpers used by both unit and composition spec
  serialization.
- The resulting `to_dict(...)` methods are now closer to the desired shape:
  adapt generated schema state, then delegate shared serialization plumbing
  rather than each owning parallel normalization logic.
- Validated with `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py -q`,
  `UV_CACHE_DIR=.uv-cache uv run ruff check src/genome_spy/chart.py
  plans/chart_layer_inventory.md`, and `UV_CACHE_DIR=.uv-cache uv run mypy
  src/`.

## 2026-07-15 - Added a concrete implementation status snapshot

- Added `plans/implementation_status.md` as a concise current-state map of the
  codebase so ongoing Altair-alignment work can be discussed in terms of
  implemented, intentional manual, transitional, missing, and deferred areas
  rather than as a stream of local refactors.
- The snapshot makes explicit that the project is already a working
  schema-backed library and that the main remaining work is architectural
  consolidation, especially around `chart.py` boundaries and generator-vs-
  handwritten ownership.

## 2026-07-15 - Kwds helper generation now covers helper-facing anyOf schema families

- Extended `tools/schemapi/codegen.py` so generated kwds helpers now include
  `DataFormat`, `DynamicOpacity`, `Parameter`, and `Parse`, and fixed the
  helper-discovery pass to avoid a recursive dependency between
  `kwds_type_names()` and the kwds-aware analyzer.
- Tightened the handwritten helper layer in `src/genome_spy/helpers.py` so
  `dynamic_opacity(...)` and `data_format(...)` now use generated kwds-backed
  `Unpack[...]` signatures directly, while `parse(...)` now advertises the
  schema-backed parse value type for arbitrary field mappings.
- Kept `param(name, ...)` ergonomic with a small explicit helper `TypedDict`
  for the non-name keywords because Python typing cannot subtract the
  positional `name` field from the generated `ParameterKwds`.
- Updated `tests/test_schema_codegen.py` to cover the real regression shape:
  helper-relevant definitions wrapped in `anyOf` branches rather than plain
  top-level objects.
- Validated with `UV_CACHE_DIR=.uv-cache uv run pytest
  tests/test_schema_codegen.py tests/test_chart.py -q`,
  `UV_CACHE_DIR=.uv-cache uv run ruff check src/genome_spy/helpers.py
  tools/schemapi/codegen.py tests/test_schema_codegen.py`, and
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`.

## 2026-07-15 - Composition resolution merging now uses shared TopLevelSpec plumbing

- Refactored composition-level `resolve_axis(...)`, `resolve_scale(...)`, and
  `resolve_legend(...)` in `src/genome_spy/chart.py` so they now route through
  shared `TopLevelSpec` helpers instead of keeping a bespoke `_merge_resolution`
  implementation on `_CompositionSpec`.
- Added shared `_merged_resolution(...)` and `_with_resolution(...)` helpers so
  the resolution path now follows the same copy-based merge pattern as the
  shared config/property/transform plumbing.
- Tightened the public resolution method signatures using the generated
  `AxisResolveKwds`, `ScaleResolveKwds`, and `LegendResolveKwds` types via
  `Unpack[...]`, which better reflects the schema-backed keyword surface in
  editors and static checking.
- Added a regression test in `tests/test_chart.py` to lock in merge behavior
  across repeated resolution calls.
- Validated with `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py -q`,
  `UV_CACHE_DIR=.uv-cache uv run ruff check src/genome_spy/chart.py
  tests/test_chart.py`, and `UV_CACHE_DIR=.uv-cache uv run mypy src/`.

## 2026-07-15 - Collapsed more duplicated chart/composition plumbing into TopLevelSpec

- Refactored `src/genome_spy/chart.py` so more repeated unit/composition
  plumbing now lives in `TopLevelSpec` instead of being duplicated between
  `Chart` and `_CompositionSpec`.
- Added shared helpers for normalized top-level property application,
  appending normalized transforms, and merging top-level config during
  copy-based builder operations.
- Removed the now-redundant duplicated implementations of
  `_append_transform(...)`, `_with_properties(...)`, and `_configure(...)` from
  the concrete chart/composition classes, while keeping data normalization,
  encoding semantics, serialization, and resolution merging in the chart layer
  where they still belong.
- This follows the preferred Altair-alignment direction already documented in
  the repo: collapse duplicate chart/composition plumbing before inventing new
  runtime abstractions.
- Validated with `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py -q`,
  `UV_CACHE_DIR=.uv-cache uv run ruff check src/genome_spy/chart.py
  tests/test_chart.py`, and `UV_CACHE_DIR=.uv-cache uv run mypy src/`.

## 2026-07-15 - Channel base now shrinks behind generated wrappers and a dedicated locus helper

- Trimmed `src/genome_spy/channels.py` so the generic handwritten `Channel`
  base now mainly owns backend behavior (`to_dict()`, `sort(...)`,
  `_with_nested(...)`, `_with_property(...)`, and replacement/copy plumbing)
  instead of also serving as the main user-facing fluent surface.
- Added a dedicated handwritten `LocusChannel` for `gs.Locus(...)` /
  `gs.locus(...)`, keeping the Altair-style fluent experience for genomic locus
  channels (`axis(...)`, `scale(...)`, `title(...)`, `chrom(...)`, `pos(...)`,
  `offset(...)`, `band(...)`) without requiring those methods to remain on the
  generic fallback `Channel` type.
- Left the generated channel classes as the primary fluent surface for ordinary
  encoding channels; they now carry their own schema-derived user-facing
  setters while the locus helper remains the only handwritten channel-specific
  authoring wrapper.
- Added regression coverage in `tests/test_chart.py` to lock in the dedicated
  `LocusChannel` return type and fluent locus-property behavior.
- Validated with `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py -q`,
  `UV_CACHE_DIR=.uv-cache uv run ruff check src/genome_spy/channels.py
  tests/test_chart.py`, and `UV_CACHE_DIR=.uv-cache uv run mypy src/`.

## 2026-07-15 - Helper layer now distinguishes stable conveniences from config wrappers

- Tightened `src/genome_spy/helpers.py` so the remaining config-adjacent
  helpers use generated kwds typing where the schema package already supports
  it: `scales(...)`, `view(...)`, `view_config(...)`, and `config(...)` now
  advertise the generated `TypedDict` contract instead of generic `Any`
  kwargs.
- Kept the helper surface unchanged while making its role more explicit:
  small constructors like `expr(...)`, `step(...)`, `parse(...)`,
  `data_format(...)`, and `param(...)` remain part of the intended ergonomic
  layer, while top-level config helpers are documented as lightweight
  compatibility wrappers around the generated `configure*` surface.
- Left `title(...)` as the intentional typing exception because the generated
  `TitleKwds` shape includes `text`, which clashes with the helper's purpose of
  supplying `text` positionally.
- Updated `docs/api.md` and `plans/altair_alignment_plan.md` so the public docs
  and architecture notes now reflect that boundary more clearly.
- Validated with `UV_CACHE_DIR=.uv-cache uv run ruff check
  src/genome_spy/helpers.py docs/api.md`, `UV_CACHE_DIR=.uv-cache uv run mypy
  src/`, and `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py -q`.

## 2026-07-15 - Generated channel setters now cover schema-derived scalar properties

- Extended `tools/schemapi/codegen.py` so generated channel wrappers now emit
  fluent setters for schema-derived non-merge properties such as `field(...)`,
  `type(...)`, `band(...)`, `value(...)`, and locus-channel `chrom(...)` /
  `pos(...)`, instead of limiting the generated surface to a small handwritten
  allowlist.
- Kept `sort(...)` on the handwritten runtime backend on purpose because it
  accepts a richer authoring surface than the current schema alone captures,
  while other nested object properties still route through generated
  merge-style setters.
- Added `Channel._with_property(...)` in `src/genome_spy/channels.py` as the
  tiny shared backend for those generated scalar setters, updated generated
  channel constructor typing to accept `SchemaBase`, and regenerated the
  checked-in schema package.
- Tightened regression coverage in `tests/test_schema_codegen.py` and
  `tests/test_chart.py`, and clarified the `TopLevelSpec.copy(...)` typing
  contract with a typing-only protocol so mypy understands the shared config
  helpers without affecting runtime MRO.
- Validated with `UV_CACHE_DIR=.uv-cache uv run pytest
  tests/test_schema_codegen.py tests/test_chart.py -q`,
  `UV_CACHE_DIR=.uv-cache uv run ruff check src/genome_spy/channels.py
  src/genome_spy/chart.py tools/schemapi/codegen.py
  src/genome_spy/schema/channels.py tests/test_schema_codegen.py
  tests/test_chart.py`, and `UV_CACHE_DIR=.uv-cache uv run mypy src/`.

## 2026-07-15 - Simplified gallery cards and trimmed docs copy

- Simplified the generated gallery card markup in
  `docs/_ext/genomespy_gallery.py` so gallery cards now show the screenshot and
  title only, which keeps the page tighter while the thumbnail visuals remain
  the main navigation cue.
- Shortened the gallery intro and category blurbs in `tools/docs_gallery.py`
  and removed the remaining Altair-specific wording from the "Basics"
  category.
- Tightened a few top-level docs pages (`README.md`, `docs/about.md`,
  `docs/getting-started.md`) so they stay focused on authoring and rendering
  GenomeSpy visualizations instead of lingering on dataset or implementation
  side notes.
- Validated with `UV_CACHE_DIR=.uv-cache uv run ruff check
  docs/_ext/genomespy_gallery.py tools/docs_gallery.py README.md docs/about.md
  docs/getting-started.md`, `UV_CACHE_DIR=.uv-cache uv run pytest
  tests/test_docs_gallery.py -q`, and `UV_CACHE_DIR=.uv-cache uv run
  sphinx-build -E -b html -W --keep-going docs docs/_build/html`.

## 2026-07-15 - Helper layer normalized onto shared runtime utilities

- Simplified `src/genome_spy/helpers.py` so transitional helpers such as
  `scales(...)` and `config(...)` now delegate more directly to shared schema
  normalization utilities instead of carrying their own special-case wrapper
  handling.
- Kept the helper surface intact while making the implementation more clearly a
  thin compatibility layer on top of generated schema objects and runtime
  helpers.
- Validated with `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py -q`,
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`, `UV_CACHE_DIR=.uv-cache uv run
  ruff check src/genome_spy/helpers.py tests/test_chart.py docs/user-guide`,
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_docs_gallery.py -q`, and
  `UV_CACHE_DIR=.uv-cache uv run sphinx-build -E -b html -W --keep-going docs
  docs/_build/html`.

## 2026-07-15 - Replaced user-guide scaffolds with current API guidance

- Rewrote the placeholder user-guide pages in `docs/user-guide/` so they now
  give short, current guidance for charts, encodings, composition, and genomic
  locus axes instead of scaffold admonitions.
- Kept the pages concise and example-driven, reflecting the current generated
  channel/config surface rather than promising future structure.
- Validated with `UV_CACHE_DIR=.uv-cache uv run ruff check docs/user-guide
  tests/test_docs_gallery.py`, `UV_CACHE_DIR=.uv-cache uv run pytest
  tests/test_docs_gallery.py -q`, and `UV_CACHE_DIR=.uv-cache uv run
  sphinx-build -E -b html -W --keep-going docs docs/_build/html`.

## 2026-07-15 - Docs now prefer generated configure surfaces

- Updated the README and docs pages to state more explicitly that generated
  `configure(...)` and `configure_* (...)` methods are the preferred way to
  author top-level chart config, while `gs.config(...)` and
  `gs.view_config(...)` remain available as lightweight schema constructors.
- Added the same preference guidance to the helper docstrings in
  `src/genome_spy/helpers.py`.
- Replaced one remaining gallery example use of `view=gs.view(...)` with
  `.configure_view(...)` in `docs/examples/genome_tracks.py` so the docs lean
  more consistently toward the generated surface.
- Validated with `UV_CACHE_DIR=.uv-cache uv run ruff check
  src/genome_spy/helpers.py docs/examples/genome_tracks.py
  tests/test_chart.py`, `UV_CACHE_DIR=.uv-cache uv run pytest
  tests/test_chart.py tests/test_docs_gallery.py -q`, and
  `UV_CACHE_DIR=.uv-cache uv run sphinx-build -E -b html -W --keep-going docs
  docs/_build/html`.

## 2026-07-15 - Shared config backend for charts and compositions

- Collapsed the duplicated nested config/property update logic in
  `src/genome_spy/chart.py` into shared `TopLevelSpec` helpers so both unit
  charts and composition specs now reuse the same config-object reconstruction
  and update path.
- Kept the generated `configure(...)` and `configure_* (...)` surface
  unchanged while removing another chunk of repeated handwritten chart-layer
  plumbing.
- Validated with `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py
  tests/test_schema_codegen.py -q`, `UV_CACHE_DIR=.uv-cache uv run mypy src/`,
  `UV_CACHE_DIR=.uv-cache uv run ruff check src/genome_spy/chart.py
  tests/test_chart.py tests/test_schema_codegen.py`,
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_docs_gallery.py -q`, and
  `UV_CACHE_DIR=.uv-cache uv run sphinx-build -E -b html -W --keep-going docs
  docs/_build/html`.

## 2026-07-15 - Unified record-like data normalization in chart helpers

- Refactored the authoring-edge data helpers in `src/genome_spy/chart.py` so
  record-like inputs now flow through one shared extraction path before both
  chart serialization and field-type inference.
- Added small helper splits for normalized record payloads, per-value type
  inference, channel-definition normalization, and default y-scale injection so
  the remaining handwritten chart plumbing is easier to reason about without
  changing the public API.
- Kept existing behavior intact for datetime JSON safety, secondary
  positional-channel type stripping, and the default reversed y scale.
- Validated with `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py -q`,
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`, `UV_CACHE_DIR=.uv-cache uv run
  ruff check src/genome_spy/chart.py tests/test_chart.py`,
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_docs_gallery.py
  tests/test_schema_codegen.py -q`, and `UV_CACHE_DIR=.uv-cache uv run
  sphinx-build -E -b html -W --keep-going docs docs/_build/html`.

## 2026-07-15 - Shared encoding merge path for charts and compositions

- Moved the duplicated `encode(...)` channel-merge workflow out of the concrete
  `Chart` and composition classes into a shared `TopLevelSpec._merged_encoding`
  helper in `src/genome_spy/chart.py`.
- Kept the public fluent API unchanged while reducing one more chunk of
  handwritten chart plumbing: positional channel-name inference, duplicate
  detection, current-encoding lookup, and final merge now live in one place for
  both unit charts and composed specs.
- Validated with `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py
  tests/test_schema_codegen.py -q`, `UV_CACHE_DIR=.uv-cache uv run ruff check
  src/genome_spy/chart.py tests/test_chart.py tests/test_schema_codegen.py`,
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`, `UV_CACHE_DIR=.uv-cache uv run
  pytest tests/test_docs_gallery.py -q`, and `UV_CACHE_DIR=.uv-cache uv run
  sphinx-build -E -b html -W --keep-going docs docs/_build/html`.

## 2026-07-15 - Channel merge plumbing moved into shared runtime helpers

- Refactored `src/genome_spy/channels.py` so the handwritten `Channel` base now
  delegates nested `axis(...)`, `scale(...)`, and `legend(...)` merge behavior
  to the shared `merge_mapping_value(...)` runtime helper in
  `src/genome_spy/schemapi.py`.
- Collapsed the remaining handwritten `sort(...)` merge path onto the same
  shared runtime semantics for mapping-like cases while preserving simple list
  and string sort values and the public error behavior.
- Normalized schema-wrapper and raw-mapping channel inputs through the shared
  schema normalization helper so the handwritten channel layer stays thinner
  and more consistent with the rest of the generated/runtime stack.
- Validated with `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py
  tests/test_schema_codegen.py -q`, `UV_CACHE_DIR=.uv-cache uv run mypy src/`,
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_docs_gallery.py -q`,
  `UV_CACHE_DIR=.uv-cache uv run ruff check src/genome_spy/channels.py
  tests/test_chart.py tests/test_schema_codegen.py`, and
  `UV_CACHE_DIR=.uv-cache uv run sphinx-build -E -b html -W --keep-going docs
  docs/_build/html`.

## 2026-07-15 - Docs examples now use generated property setters

- Updated several gallery examples to use the new generated fluent setter
  surface directly on shared schema wrappers such as `Scale`, `Legend`, and
  `GenomeAxis`, replacing some remaining verbose constructor-heavy objects.
- Applied the new style in representative examples across copy-number,
  association, lollipop, admixture, and oncoplot pages so the docs now reflect
  the generated ergonomic layer we just added to the runtime.
- Revalidated the docs/gallery path with `UV_CACHE_DIR=.uv-cache uv run ruff
  check docs/examples tests/test_docs_gallery.py src`,
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_docs_gallery.py -q`, and
  `UV_CACHE_DIR=.uv-cache uv run sphinx-build -E -b html -W --keep-going docs
  docs/_build/html`.

## 2026-07-15 - Altair-style generated property setters

- Added an Altair-style `with_property_setters` decorator to
  `src/genome_spy/schemapi.py` and taught generated schema classes to use it,
  so object wrappers like `Legend`, `Scale`, and `GenomeAxis` now expose direct
  fluent setters such as `.title(...)`, `.padding(...)`, `.zero(...)`, and
  `.grid(...)` in addition to the existing `with_*` methods.
- Kept the implementation lightweight and local to the schema runtime:
  generated classes resolve referenced properties through the packaged schema,
  then the decorator installs descriptor-backed setters only for valid Python
  identifiers.
- Regenerated the checked-in schema package so the new decorator-backed
  surface is available across `src/genome_spy/schema/core.py`.
- Added regression coverage for both code generation and runtime behavior of
  the new property-setter surface, and tightened the generated-channel test to
  distinguish generator-owned methods from inherited base-channel helpers.
- Validated with `UV_CACHE_DIR=.uv-cache uv run pytest
  tests/test_generated_schema_package.py tests/test_schema_codegen.py -q`,
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py
  tests/test_docs_gallery.py -q`, `UV_CACHE_DIR=.uv-cache uv run ruff check
  src tests tools`, `UV_CACHE_DIR=.uv-cache uv run mypy src/`, and
  `UV_CACHE_DIR=.uv-cache uv run sphinx-build -E -b html -W --keep-going docs
  docs/_build/html`.

## 2026-07-15 - Docs gallery concision and generated config usage

- Updated several gallery examples to prefer generated `configure_view(...)`
  calls over top-level `view=gs.view(...)` when the examples are single-root
  charts, keeping the docs aligned with the newer generated config surface.
- Tightened example descriptions so gallery pages focus on the visualization
  pattern rather than repeatedly mentioning source provenance or hosted data.
- Revalidated that the docs example files still drive the rendered gallery
  output directly by running the gallery test and a full Sphinx rebuild.
- Validated with `UV_CACHE_DIR=.uv-cache uv run ruff check docs/examples
  tests/test_docs_gallery.py src`, `UV_CACHE_DIR=.uv-cache uv run pytest
  tests/test_docs_gallery.py -q`, and `UV_CACHE_DIR=.uv-cache uv run
  sphinx-build -E -b html -W --keep-going docs docs/_build/html`.

## 2026-07-15 - Fluent sort helpers and docs example cleanup

- Added a handwritten `gs.compare(...)` helper backed by the generated
  `CompareParams` schema wrapper, plus a fluent `Channel.sort(...)` method so
  channel ordering can be authored in a more Altair-like style instead of raw
  nested dicts.
- Added a small `gs.scales(...)` helper for top-level shared-scale
  configuration and updated docs examples to prefer fluent
  `gs.Locus(...).scale(...).axis(...)` chaining over raw nested locus dicts.
- Added tiny `gs.view(...)` and `gs.config(...)` helpers so the docs can avoid
  raw nested view/config dicts while still respecting GenomeSpy's composed-root
  schema constraints.
- Extended `transform_collect(...)` and `transform_stack(...)` to accept schema
  wrappers for `sort`, not just raw mappings, keeping transform ergonomics
  aligned with channel ergonomics.
- Added `plans/altair_alignment_plan.md` and expanded it with a concrete
  codegen worklist comparing the current generator/runtime/API split to
  Altair's richer generated ergonomic layer.
- Recorded Altair decorator ideas worth recycling next: `use_signature` as a
  low-risk first improvement for generated helper methods, and
  `with_property_setters` as the larger follow-up move for richer generated
  channel ergonomics.
- Implemented the first decorator-backed slice by adding a small
  `use_signature` utility in `src/genome_spy/schemapi.py` and teaching the
  generator to apply schema-derived signatures to generated `mark_*` methods
  via mark-specific `*Props` classes where available.
- Updated focused docs examples to use the new compare helper and replaced a
  few remaining raw config patterns with existing helpers (`mark_rule(...)`,
  `gs.step(...)`) in the GFF3 and genome-track gallery examples.
- Added regression coverage for fluent channel sorting, helper construction,
  and transform helper interoperability.
- Validated with `UV_CACHE_DIR=.uv-cache uv run ruff format ...`,
  `UV_CACHE_DIR=.uv-cache uv run ruff check src tests docs/examples`,
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`, and
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py tests/test_docs_gallery.py -q`
  (72 passed).

## 2026-07-15 - Moved reusable chart normalization plumbing into `schemapi`

- Added three shared runtime helpers to `src/genome_spy/schemapi.py`:
  - `normalize_schema_value(...)`
  - `normalize_mapping_value(...)`
  - `merge_mapping_value(...)`
- These helpers now own the generic schema-wrapper normalization and nested
  builder-style merge semantics that had previously lived only inside
  `src/genome_spy/chart.py`.
- Updated `chart.py` to reuse those runtime helpers for:
  - top-level `.properties(...)` normalization,
  - transform kwarg normalization,
  - generated config/configure merge behavior.
- Deliberately kept chart-specific logic such as data normalization, field-type
  inference, and encoding-specific defaults in `chart.py`; those are workflow
  concerns rather than generic schema runtime behavior.
- Preserved the existing chart-facing error message for unsupported generic
  transforms so the refactor stays behavior-preserving from the public API's
  perspective.
- Updated `plans/altair_alignment_plan.md` so `G3` is now partially addressed
  rather than fully open.
- Validation: `UV_CACHE_DIR=.uv-cache uv run ruff check src tests tools`,
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`, and
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py tests/test_schema_codegen.py tests/test_docs_gallery.py -q`.

## 2026-07-15 - Channel nested-setter generation is now schema-driven

- Replaced the hardcoded channel nested-setter allowlist in
  `tools/schemapi/codegen.py` with schema-driven discovery from the resolved
  encoding definitions.
- The new discovery logic only emits fluent nested setters when a channel
  property is:
  - backed by an object-like schema definition, and
  - compatible with merge-style builder semantics rather than being a mixed
    scalar/object union or a plain alias like `FieldName`.
- Regenerated `src/genome_spy/schema/channels.py` with that new logic and added
  regression coverage in `tests/test_schema_codegen.py` using a synthetic
  schema that proves newly introduced object-like properties get setters
  automatically while incompatible properties do not.
- This is mostly an architectural improvement today: the current upstream
  GenomeSpy encoding schema still exposes only a small number of compatible
  nested channel objects, so the visible generated API does not explode, but
  future schema additions will now surface automatically without another
  allowlist edit.
- Validation: `UV_CACHE_DIR=.uv-cache uv run ruff check src tests tools`,
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`, and
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_schema_codegen.py tests/test_chart.py -q`.

## 2026-07-15 - Docs now start preferring generated config methods

- Updated `docs/examples/bam_read_alignments.py` to use the new generated
  `.configure_view(...)` surface instead of routing a simple config-only change
  through `gs.config(view=...)`.
- Added a regression test in `tests/test_chart.py` that verifies the generated
  config method path serializes the same top-level config as the helper-backed
  path, which gives us a safe migration path for docs/examples.
- This does not remove the helpers; it starts shifting the examples toward the
  generated config surface where it is clearly simpler and more systematic.
- Validation: `UV_CACHE_DIR=.uv-cache uv run ruff check src tests docs/examples/bam_read_alignments.py tools`,
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`, and
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py tests/test_schema_codegen.py -q`.

## 2026-07-15 - Landed the first generated config ergonomics surface

- Extended `tools/schemapi/codegen.py` so `src/genome_spy/schema/mixins.py`
  now generates a `ConfigMethodMixin` alongside `MarkMethodMixin`.
- The new generated config surface is schema-driven from `GenomeSpyConfig` and
  currently exposes:
  - `configure(...)` for whole-config merges,
  - `configure_axis(...)`, `configure_view(...)`, `configure_mark(...)`, and
    the other `GenomeSpyConfig` property families.
- Wired `ConfigMethodMixin` into both `Chart` and composition specs in
  `src/genome_spy/chart.py`, with a small handwritten backend for merging
  nested config objects in one place rather than spreading config logic across
  many helper functions.
- Added regression coverage for both generator output and runtime behavior:
  - generated mixins are asserted in `tests/test_schema_codegen.py`,
  - chart/composition `configure*` behavior is asserted in
    `tests/test_chart.py`.
- Updated `plans/altair_alignment_plan.md` so config ergonomics gap `G1` is
  now marked partially addressed rather than fully open.
- Validation: `UV_CACHE_DIR=.uv-cache uv run ruff check src tests tools`,
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`, and
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_schema_codegen.py tests/test_chart.py -q`.

## 2026-07-15 - Added an explicit Altair-alignment ownership matrix

- Expanded `plans/altair_alignment_plan.md` from a direction document into a
  working implementation map with three concrete sections:
  - an ownership matrix for what should stay handwritten,
  - a transitional list for what should shrink over time,
  - an ordered gap list for what is still missing relative to the intended
    generated-versus-handwritten split.
- The matrix now makes the next steps much clearer:
  - keep chart/composition workflows, locus helpers, data entry points, and
    notebook/export behavior handwritten,
  - continue moving channel ergonomics and config-family schema coverage toward
    generation,
  - treat helper growth as an exception that must justify itself against that
    matrix.
- This gives the repo a cleaner implementation picture without forcing any
  immediate code churn, which was the main goal of this planning pass.

## 2026-07-15 - Top-level properties now normalize schema wrappers consistently

- Extended `TopLevelSpec.properties(...)` in `src/genome_spy/chart.py` so it
  now normalizes `SchemaBase` values recursively before copying them into chart
  state, matching the treatment already used for channels, data, and
  transforms.
- This makes top-level authoring more consistent: properties such as
  `config=gs.config(...)`, `view=gs.view(...)`, `title=gs.title(...)`, and
  parameter objects can now be passed through `.properties(...)` as schema
  wrappers without relying on later incidental serialization steps.
- Updated regression coverage in `tests/test_chart.py` to exercise schema
  wrappers inside `.properties(...)`, including top-level config, top-level
  view background, and parameter helpers together in one chart.
- Updated `docs/examples/bam_read_alignments.py` to use the clearer
  `gs.view_config(...)` helper for config-side view defaults.
- Validation: `UV_CACHE_DIR=.uv-cache uv run ruff check src tests docs/examples/bam_read_alignments.py`,
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`, and
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py tests/test_schema_codegen.py -q`.

## 2026-07-15 - Config and view helpers now prefer schema-backed objects

- Tightened the handwritten helper layer in `src/genome_spy/helpers.py` so
  common config/view helpers now return generated schema wrappers instead of
  anonymous dicts where the schema surface is already available.
- `gs.view(...)` now returns `ViewBackground`, which matches the top-level
  `view=` property accepted by unit and composition specs.
- Added `gs.view_config(...)` for the distinct top-level config-only
  `ViewConfig` object, making the two different GenomeSpy "view" concepts
  explicit in the API.
- `gs.config(...)` now returns a generated `GenomeSpyConfig` wrapper and
  preserves current docs/example ergonomics by coercing a `ViewBackground`
  passed as `view=` into the corresponding `ViewConfig`.
- Kept `gs.scales(...)` as a lightweight helper because the schema models
  shared scales as a typed mapping rather than a generated object class; the
  helper is now typed against `ScalesKwds` to reflect that more clearly.
- Updated public exports and regression coverage so helper constructors are now
  asserted as schema-backed values while still serializing to the same JSON
  shapes expected by existing examples.
- Validation: `UV_CACHE_DIR=.uv-cache uv run ruff check src tests tools`,
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`, and
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py tests/test_schema_codegen.py -q`.

## 2026-07-15 - Generated channels now own schema-backed title ergonomics

- Extended `tools/schemapi/codegen.py` so channel wrapper generation now
  derives fluent channel setters directly from encoding schema properties
  instead of keeping all lightweight ergonomics on the handwritten base class.
- Generated channel classes in `src/genome_spy/schema/channels.py` now emit
  `title(...)` methods wherever the upstream channel schema exposes `title`,
  which moves the visible API shape closer to the generated-wrapper pattern
  used by Altair.
- Added schema-driven support for emitting generated `sort(...)` methods when a
  channel schema declares `sort`; the current GenomeSpy encoding schema does
  not expose channel `sort`, so this is a generation capability we are ready to
  pick up automatically when the schema grows there.
- Kept the handwritten `Channel.title(...)` and `Channel.sort(...)` logic as
  the implementation backend for now so the generated methods stay small and we
  do not duplicate merge/null handling while the API surface is still settling.
- Added regression coverage in `tests/test_schema_codegen.py` for generated
  channel setters, including a synthetic schema case that verifies `sort(...)`
  emission and a runtime check that `gs.X`/`gs.Color` now define `title(...)`
  on the generated classes themselves.
- Validation: `UV_CACHE_DIR=.uv-cache uv run ruff check src tests tools`,
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`, and
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_schema_codegen.py tests/test_chart.py tests/test_docs_gallery.py -q`.

## 2026-07-14 - Altair-style authoring helpers and channel ergonomics

- Expanded the handwritten public API toward Altair's authoring model by
  re-exporting the missing generated channel wrappers that were already useful
  in docs examples, including `Dx`, `Dy`, `Fill`, `Shape`, `Search`, `Stroke`,
  `Angle`, and related channels.
- Added small top-level helper constructors in `genome_spy.helpers` for common
  schema objects that had been forcing raw dicts in examples:
  `expr(...)`, `title(...)`, `step(...)`, `dynamic_opacity(...)`,
  `data_format(...)`, `parse(...)`, and `param(...)`.
- Re-exported the most relevant generated schema wrappers at the package top
  level as part of the same authoring surface, including `ExprRef`, `Title`,
  `Step`, `DynamicOpacity`, `DataFormat`, `Parse`, `Parameter`, `Scale`,
  `Legend`, and `GenomeAxis`.
- Taught channel normalization to accept generated schema wrappers directly,
  which lets helpers such as `gs.expr(...)` flow through channel constructors
  without dropping back to manual dicts.
- Added composition-level `.encode(...)` support so layered specs can carry
  shared encodings directly, mirroring Altair more closely and avoiding
  `properties(encoding={...})` in docs examples.
- Rewrote the most verbose gallery examples (`genome_tracks`,
  `gff3_gene_annotations`, `sashimi_plot`, and `indexed_fasta_sequence`) to use
  the new channel wrappers and helpers instead of nested encoding dicts where
  the ergonomic API can now express the same intent.
- Tightened the docs gallery thumbnail path so release/manual docs builds can
  require real PNG screenshots instead of silently falling back to the generic
  SVG posters; local and push builds still allow the fallback while
  `GENOME_SPY_DOCS_REQUIRE_PNG_THUMBS=1` now fails fast if screenshots were not
  rendered.
- Validation: `UV_CACHE_DIR=.uv-cache uv run ruff format` on the touched files,
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py tests/test_docs_gallery.py -q`
  (67 passed), `UV_CACHE_DIR=.uv-cache uv run ruff check src/genome_spy
  docs/examples tests/test_chart.py tests/test_docs_gallery.py`,
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`, and
  `UV_CACHE_DIR=.uv-cache uv run sphinx-build -b html -W --keep-going docs
  docs/_build/html` all passed.

## 2026-07-14 - Plot-family gallery taxonomy and remaining example cleanup

- Reclassified the docs gallery away from dataset/domain buckets and toward
  plot-family categories in `tools/docs_gallery.py`, including:
  `Association plots`, `Volcano and MA plots`, `Genome browser tracks`,
  `Reference annotation tracks`, `Lollipop and pathogenicity plots`,
  `Oncoprints and cohort summaries`, `Copy-number plots`, and
  `Population structure plots`.
- Updated all current example `META["category"]` values under `docs/examples/`
  to match the new plot-family taxonomy, keeping domain and implementation
  details in tags rather than top-level categories.
- Finished a second pass over remaining docs-example encoding dicts in
  `bam_read_alignments`, `bigbed_ccre_track`, `clinvar_variants`, and
  `cytobands`, replacing repeated raw channel/format/title/step patterns with
  the new ergonomic helpers where the API can express them cleanly.
- Kept two deliberate escape hatches where the current handwritten layer still
  falls short of full Altair-style chaining:
  `Locus(..., axis={...})` still uses inline keyword configuration because
  `Locus()` returns a plain channel helper rather than a fluent generated
  wrapper, and `encoding={"x2": None}` remains in the cytoband separator
  layer because `X2(None)` is not yet supported.
- Updated docs-gallery regression assertions to match the new visible category
  captions.
- Validation: `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_docs_gallery.py -q`
  (26 passed), `UV_CACHE_DIR=.uv-cache uv run ruff check src/genome_spy
  tools/docs_gallery.py docs/examples tests/test_docs_gallery.py`, and
  `UV_CACHE_DIR=.uv-cache uv run sphinx-build -b html -W --keep-going docs
  docs/_build/html` all passed.

## 2026-07-14 - Fluent locus channels and nullable secondary encodings

## 2026-07-14 - Real PNG gallery thumbnails from rendered charts

- Reworked `tools/render_thumbnails.py` so gallery cards are generated from
  real Playwright screenshots of the live GenomeSpy charts rather than relying
  on the fallback poster SVGs.
- The renderer now fits each example into the fixed thumbnail card using the
  example's natural width plus its measured rendered height, which avoids the
  worst clipping for taller multi-panel examples.
- Switched the browser capture flow to wait for the chart to mount, let network
  requests go idle, pause for an additional settle window, and then capture the
  fixed page viewport. This is more reliable for lazy BigWig/BED/annotation
  examples than the earlier immediate screenshot path.
- Added `tests/test_render_thumbnails.py` to lock in the thumbnail layout
  helper, and updated the PNG-required gallery test to use a temporary
  thumbnail directory now that real PNGs exist in-tree.
- Rendered fresh PNG thumbnails for all 18 docs examples under
  `docs/_static/gallery/`.

- Extended the base handwritten `Channel` helper to support fluent
  `.axis(...)`, `.scale(...)`, and `.legend(...)` configuration, so
  `Locus(...)` can participate in the same Altair-style chaining pattern as the
  generated channel wrappers for common docs use cases.
- Taught chart encoding normalization to accept `None` directly for encoding
  values, which allows `.encode(x2=None)` / `.encode(y2=None)` as a clean
  null-secondary-channel escape hatch instead of requiring raw `encoding={...}`
  dicts.
- Updated the remaining docs examples that depended on those missing features:
  the ClinVar example now uses fluent `Locus(...).axis(...)`, and the cytoband
  separator layer now uses `.encode(x2=None)`.
- Added regression coverage for fluent locus axis/scale configuration and
  nullable secondary channels in `tests/test_chart.py`.
- Validation: `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py
  tests/test_docs_gallery.py -q` (70 passed), `UV_CACHE_DIR=.uv-cache uv run
  mypy src/`, `UV_CACHE_DIR=.uv-cache uv run ruff check src/genome_spy
  docs/examples tests/test_chart.py tests/test_docs_gallery.py`, and
  `UV_CACHE_DIR=.uv-cache uv run sphinx-build -b html -W --keep-going docs
  docs/_build/html` all passed.

- Regenerated the gallery pages after noticing that
  `docs/gallery/clinvar_variants.md` was stale relative to
  `docs/examples/clinvar_variants.py`; this was why the rendered docs still
  showed the old source with `axis: None` and without the newly added inline
  comments.
- Kept the ClinVar example source lightly commented around the baseline,
  lollipop-style layering, lazy VCF source, and classification normalization so
  the docs snippet now gives readers a usable map of the example.
- Reworked the gallery taxonomy to avoid using `Genome tracks` as a catch-all:
  `Genome browsers` now groups stacked/shared-axis browser views,
  `Reference annotations` groups ideogram/sequence/regulatory reference views,
  and `Variants` now includes the ClinVar pathogenicity track.
- Updated `plans/example_gallery.md` with the working category plan so future
  additions follow the same browsing-oriented grouping.
- Validation: `uv run ruff check docs/examples/clinvar_variants.py tools/docs_gallery.py`
  and `uv run sphinx-build -E -b html -W --keep-going docs docs/_build/html`.

## 2026-07-14 - Airway examples now use concise library-backed stats and shared wrangling

- Replaced the duplicated hand-rolled airway RNA-seq analysis code in
  `airway_volcano_plot.py` and `airway_ma_plot.py` with a shared internal helper
  at `src/genome_spy/datasets/_airway.py`.
- Narrowed the helper to data loading and pandas reshaping only
  (`airway_paired_logcounts(...)`) so the example files still show the key
  statistical steps directly.
- The example pages now use SciPy's `ttest_rel` for per-gene paired tests and
  statsmodels' `fdrcorrection(...)` for Benjamini-Hochberg adjustment in a few
  short lines rather than a larger home-grown implementation.
- Switched the MA and volcano coloring back to a raw-p-value threshold while
  still computing adjusted p-values for reference; with only four matched
  pairs, FDR-only coloring flattened the examples into almost all grey points.
- Added SciPy and statsmodels to the docs/dev dependency groups so the gallery
  examples build in a library-backed way rather than reimplementing core stats.
- Added focused regression coverage for the shared airway helper in
  `tests/test_datasets.py`.
- Validation: `uv run ruff check`, `uv run pytest tests/test_datasets.py -q`,
  `uv run mypy src/`, and
  `uv run sphinx-build -E -b html -W --keep-going docs docs/_build/html`.

## 2026-07-13 - Replaced synthetic genome-track gallery example

- Reworked `docs/examples/genome_tracks.py` to remove the made-up Gaussian
  coverage and hand-authored gene blocks.
- Replaced the old example with a real hg38 browser view composed from
  GenomeSpy's own documentation examples: the lazy BigWig GC-content track and
  the scored RefSeq gene-annotation track.
- Kept the canonical RefSeq exon packing, label scoring, and strand arrows from
  GenomeSpy's docs so the example matches upstream annotation behavior instead
  of approximating it locally.
- Validated the gallery import path with `tools.docs_gallery.collect_example`,
  ran `uv run ruff check docs/examples/genome_tracks.py`, and rebuilt the docs
  with `uv run sphinx-build -E -b html -W --keep-going docs docs/_build/html`.

## 2026-07-13 - Planned Altair-style genomic API expansion

- Reviewed Altair's top-level chart mixins and transform/data handling as the
  primary API-shape reference for extending `genome-spy-python`.
- Reviewed `gos` as the genomics-specific companion reference, especially its
  lightweight data constructors and thin track transform helpers.
- Wrote `plans/genomic_api_plan.md`, a milestone-based roadmap for bringing
  lazy data sources, richer top-level genomic config, and GenomeSpy-native
  transform ergonomics into the handwritten chart API.

## 2026-07-13 - Milestone 1 implementation pass

- Confirmed that the existing `properties(...)` path already supports root- and
  composition-level GenomeSpy configuration such as `assembly`, `scales`,
  `spacing`, `view`, `viewportHeight`, and `params`; the main gap was missing
  tests and real-example usage.
- Added focused chart tests covering top-level genomic config on both unit
  charts and composed charts, including composition-level `resolve_scale(...)`
  and `resolve_axis(...)`.
- Refactored `docs/examples/genome_tracks.py` so the top-level view and layered
  structure now use `Chart`, `layer`, `vconcat`, `properties(...)`, and
  resolution helpers instead of `Root(...)`; raw dicts remain only for lazy
  data and custom transform payloads, which are Milestones 2 and 3.
- Validated with `uv run pytest tests/test_chart.py -q`, `uv run ruff check
  docs/examples/genome_tracks.py tests/test_chart.py`, and
  `uv run sphinx-build -E -b html -W --keep-going docs docs/_build/html`.

## 2026-07-13 - Milestone 2 implementation pass

- Added `src/genome_spy/data.py` with a small schema-backed data-source helper
  surface: `gs.Data(...)` plus `gs.lazy.*` builders for lazy GenomeSpy sources
  such as BigWig, BigBed, BAM, GFF3, indexed FASTA, VCF, and a generic
  `lazy.source(...)` escape hatch.
- Updated chart data normalization so `Chart(data=...)` accepts generated
  schema wrappers directly, allowing `gs.Data(...)` and `gs.lazy.*(...)` to be
  passed without dropping to raw dictionaries.
- Exported `Data` and `lazy` from the public package and migrated the genome
  tracks example to use `gs.lazy.bigwig(...)` and `gs.Data(...)` for its data
  declarations.
- Added focused tests for schema-backed data wrappers and lazy helper
  serialization, and validated with `uv run pytest tests/test_chart.py -q`,
  `uv run ruff check ...`, `uv run mypy src/`, and a full docs build.

## 2026-07-13 - Milestone 3 implementation pass

- Added a generic `TopLevelSpec.transform(...)` method that accepts one or more
  arbitrary GenomeSpy transform mappings and appends them in order, giving the
  handwritten API a durable escape hatch for transforms that do not yet have
  dedicated helpers.
- Extended transform normalization to accept generated schema wrappers in
  addition to plain mappings, keeping the generic transform path aligned with
  the schema-backed API design.
- Refactored `docs/examples/genome_tracks.py` to use `.transform(...)` together
  with the existing formula/filter helpers instead of embedding large raw
  transform arrays directly in chart constructors.
- Added focused tests for generic transform chaining, raw mapping
  serialization, unsupported-value rejection, and interoperability with the
  existing handwritten transform helpers.
- Validated with `uv run pytest tests/test_chart.py -q`, `uv run ruff check
  src/genome_spy/chart.py docs/examples/genome_tracks.py tests/test_chart.py`,
  `uv run mypy src/`, and a full docs build.

## 2026-07-13 - Milestone 4 first helper wave

- Promoted the repeated GenomeSpy transforms from the genome-track example into
  named handwritten helpers on `TopLevelSpec`: `transform_collect(...)`,
  `transform_project(...)`, `transform_pileup(...)`,
  `transform_linearize_genomic_coordinate(...)`,
  `transform_measure_text(...)`, `transform_filter_scored_labels(...)`,
  `transform_flatten(...)`, and `transform_flatten_compressed_exons(...)`.
- Kept these helpers deliberately thin wrappers over the generic transform path
  so the API stays schema-aligned and easy to extend.
- Refactored `docs/examples/genome_tracks.py` again so it now uses those named
  helpers instead of raw transform dictionaries for the repeated GenomeSpy
  transform patterns.
- Added focused serialization tests for the new helper methods and revalidated
  the updated example flow with `uv run pytest tests/test_chart.py -q`,
  `uv run ruff check src/genome_spy/chart.py docs/examples/genome_tracks.py
  tests/test_chart.py`, `uv run mypy src/`, and a full docs build.

## 2026-07-13 - Milestone 5 validation on a second GenomeSpy docs example

- Added `docs/examples/gff3_gene_annotations.py`, a second real GenomeSpy docs
  example adapted to the handwritten API surface. The example exercises lazy
  GFF3 loading, top-level genomic config, layered marks, and the new
  transform-helper wave on a richer annotation track than the RefSeq example.
- Confirmed that the new example serializes cleanly through
  `tools.docs_gallery.collect_example(...)` and that the generated gallery now
  includes `gallery/gff3_gene_annotations`.
- Revalidated the docs site with `uv run ruff check
  docs/examples/gff3_gene_annotations.py src/genome_spy/chart.py`,
  `uv run mypy src/`, and `uv run sphinx-build -E -b html -W --keep-going docs
  docs/_build/html`.

## 2026-07-13 - Added remaining GenomeSpy genomic-track gallery examples

- Added GenomeSpy-docs-derived gallery examples for the remaining practical
  genomic track patterns currently supported by the API surface:
  `cytobands.py`, `sashimi_plot.py`, `bigbed_ccre_track.py`,
  `indexed_fasta_sequence.py`, `clinvar_variants.py`, and
  `bam_read_alignments.py`.
- Fixed composition-level API mismatches encountered during those ports,
  especially cases where layer/vconcat roots require `properties(encoding=...)`
  rather than `.encode(...)`, and a cytoband separator override that needed
  `x2: null` instead of a value channel.
- Verified that every example under `docs/examples/` now imports through
  `tools.docs_gallery.collect_example(...)` and that the docs gallery builds
  successfully with the expanded Genome tracks category.

## 2026-07-13 - Fix composition resolution serialization for docs gallery

- Traced the oncoprint runtime error about mixed shared `x` data types to the
  handwritten composition helpers serializing `resolve_scale(...)` into the
  `scales` property instead of the schema's `resolve.scale` block.
- Corrected `resolve_scale(...)`, `resolve_axis(...)`, and
  `resolve_legend(...)` to write nested `resolve` entries using GenomeSpy's
  `shared` / `independent` resolution behavior values.
- Updated chart regression coverage to assert the proper `resolve` structure
  and verified that the richer docs oncoprint example now builds cleanly again.
- Validation: `env UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py tests/test_docs_gallery.py tests/test_datasets.py -q`,
  `env UV_CACHE_DIR=.uv-cache uv run ruff check src/genome_spy/chart.py tests/test_chart.py docs/examples/oncoprint.py`,
  and `env UV_CACHE_DIR=.uv-cache uv run sphinx-build -E -b html -W --keep-going docs docs/_build/html`.
  and `uv run mypy src/`.
- `uv build` could not be validated in this session because the environment was
  unable to reach PyPI to resolve `hatchling`.

## 2026-07-13 - Split stacked airway RNA-seq gallery example

- Replaced the combined airway differential-expression gallery example with two
  separate examples: a standalone volcano plot and a standalone MA plot.
- Moved the shared paired-difference and Benjamini-Hochberg computation into a
  private `docs/examples/_airway_de.py` helper so the two views reuse the same
  derived data without copy-pasted analysis logic.
- Removed the misleading linked-zoom `vconcat` layout, since the volcano and
  MA plots do not share an x-axis and should not behave like a coordinated
  multi-panel view.
- Updated gallery tests to expect the new example names and detail-page embed.
- Validation: `env UV_CACHE_DIR=.uv-cache uv run pytest tests/test_docs_gallery.py -q`,
  `env UV_CACHE_DIR=.uv-cache uv run ruff check docs/examples/_airway_de.py docs/examples/airway_volcano_plot.py docs/examples/airway_ma_plot.py tests/test_docs_gallery.py`,
  and `env UV_CACHE_DIR=.uv-cache uv run sphinx-build -E -b html -W --keep-going docs docs/_build/html`.

## 2026-07-13 - Iframe sizing: fill container like GenomeSpy's docs

- The iframe "resize jump" came from a wrong approach. GenomeSpy's own doc specs
  set no `width`/`height` (0 of 86), so charts fill their container; our specs
  set explicit sizes via `.properties(width=, height=)`, which forced a fixed
  render size and the mismatch. Verified: removing width/height makes GenomeSpy
  fill the container exactly (both unit and vconcat charts).
- Removed all `width`/`height` from the example `.properties()` (and per-track
  sizes), gave each example an iframe `height` via `META`, and set the iframe to
  `width:100%` with the chart filling it (`#c{width:100%;height:100%}`). No
  runtime measurement, no ResizeObserver/postMessage, no layout jump.
- Deleted the build-time measurement machinery (`chart_sizes.json`,
  `gallery-resize.js`, the size helpers) that I had started; simplified
  `render_thumbnails.py` to screenshot the sizeless charts filling a fixed box.
- Validation: `uv run pytest tests/ -q` (60 passed), `uv run mypy src/`, and
  `uv run sphinx-build -b html -W --keep-going` (exit 0); screenshots confirm
  each chart fills its iframe with no empty space or resize.

## 2026-07-13 - Reveal-on-stable, two new examples, data-type plan

- The fill approach still showed a size change: GenomeSpy paints at ~95% then
  grows the canvas to 100% ~1.5s later (an internal two-phase render present in
  every container/spec configuration — confirmed via Playwright). Fixed the
  *visible* jump by keeping the chart hidden (opacity 0, with a "Loading chart…"
  label) until its canvas size is stable across two polls, then fading it in.
  Verified the reveal fires only after the size settles.
- Added two new real-data examples in a new "Variants" category: `oncoprint`
  (real TCGA BRCA1/BRCA2 alterations) and `needle_plot` (a PIK3CA lollipop with a
  functional-domain band), both from Plotly's MIT Dash Bio datasets (upstream
  cBioPortal/TCGA and UniProt). Vendored `tcga_oncoprint.json` and
  `pik3ca_mutations.json` with provenance/licenses in the data README.
- Reviewed the newly cloned references (maftools, pyGenomeTracks, pyGenomeViz,
  qqman) and the vendored airway RNA-seq counts. Wrote
  `plans/gallery_datatypes_plan.md`: a feasibility matrix over the maintainer's
  data-type taxonomy (GWAS, interval, signal, MAF, CNV, methylation, RNA-seq DE,
  single-cell, Hi-C/links) mapping each to a real dataset, a GenomeSpy recipe,
  and a now/needs-feature/deferred rating. Key gate: indexed formats (bigWig,
  bigBed, tabix) are deferred until we wrap GenomeSpy's lazy-data loaders — the
  single highest-leverage feature. Airway → a real RNA-seq volcano is the top
  next build.
- Validation: `uv run pytest tests/ -q` (62 passed), `uv run ruff check`,
  `uv run mypy src/`, and `uv run sphinx-build -b html -W --keep-going` (exit 0);
  screenshots confirm the oncoprint and lollipop render and the reveal is
  jump-free.

## 2026-07-13 - Airway RNA-seq gallery example

- Added `docs/examples/airway_differential_expression.py`, a real-data RNA-seq
  gallery example using the vendored airway counts and sample metadata.
- Computed paired treated-vs-control log-expression differences across cell
  lines, a lightweight variance-based p-value, Benjamini-Hochberg adjustment,
  and differential-expression classes for volcano and MA views.
- Updated dataset provenance and the gallery data-type plan so the airway files
  are documented as actively used by the gallery.

## 2026-07-13 - Gallery navigation and iframe stabilization

- Changed the generated gallery index to emit visible category toctrees instead
  of a hidden detail-page toctree, so PyData's section navigation updates when
  new examples are added.
- Moved gallery source generation from `builder-inited` to `config-inited` so a
  clean first Sphinx build discovers newly generated gallery pages before
  source reading and sidebar construction.
- Hardened the iframe embed document against load-time geometry shifts by fixing
  the inner document body to the viewport, absolutely positioning the chart
  container, and waiting for both canvas width and height to stabilize before
  revealing the chart.
- Added docs-gallery regression tests for visible generated navigation and the
  iframe stabilization script.

## 2026-07-13 - Packaged dataset loader

- Added a small public `genome_spy.datasets` module with `available_datasets()`
  and `load_dataset(...)` for the real vendored gallery datasets.
- Moved the vendored CSV and JSON files into the package so the dataset loader
  works from installed wheels rather than depending on `docs/_static/data/`.
- Updated the local-data gallery examples to load packaged datasets through the
  new helper instead of reading files directly from the docs tree.

## 2026-07-13 - New local example-source mapping

- Indexed `tmp/maftools`, `tmp/pyGenomeTracks`, and `tmp/pyGenomeViz` as active
  reference sources for upcoming gallery work.
- Recorded maftools extdata + `.Rd` pages as the main source for realistic
  oncoprint, rainfall, lollipop, mutation-burden, and CNV example semantics.
- Recorded pyGenomeTracks as the clearest local track-layout reference and
  pyGenomeViz fastANI as a future comparative-genomics `link` reference.

## 2026-07-13 - TCGA LAML oncoprint refresh

- Replaced the toy BRCA1/BRCA2 oncoprint gallery example with a curated TCGA
  LAML cohort view derived from `tmp/maftools/inst/extdata/tcga_laml.maf.gz`
  and `tcga_laml_annot.tsv`.
- Vendored a packaged `tcga_laml_oncoprint` dataset containing the top 12
  recurrently altered genes, sample-level FAB classes, and a collapsed
  sample-gene mutation matrix suitable for docs builds and installed-package
  examples.
- Rebuilt the example as a composed chart with a FAB annotation strip, the main
  oncoprint matrix, and a per-gene altered-sample frequency side panel.
- Updated dataset provenance notes and added focused dataset-loader regression
  coverage for the curated LAML payload.

## 2026-07-13 - QQ plot polish

- Refined the QQ plot example to distinguish the null-like bulk from the
  upward association tail with a legend and on-plot text labels.
- Added an opt-in `max_width` gallery metadata field so compact examples can
  render in a narrower iframe instead of occupying the full content width.
- Applied the new width cap to the QQ plot detail page and added regression
  coverage for the generated iframe style.

## 2026-07-13 - Gallery category cleanup and ClinVar page regeneration

## 2026-07-07 - Migrated docs from MkDocs to Sphinx

- Maintainer asked to move off MkDocs to Sphinx and to add an Altair-style
  landing-page "art gallery". Studied the Altair docs in `tmp/altair` (its
  `sphinxext_altair` extension, `altair-minigallery` directive, and
  `#showcase`/`.gallery` CSS) as the reference implementation.
- Replaced the MkDocs stack with Sphinx + pydata-sphinx-theme + myst-parser +
  sphinx-design + sphinx-copybutton. Swapped the `docs` dependency group and
  relocked; removed `mkdocs.yml` and `tools/mkdocs_hooks.py`.
- Refactored `tools/docs_gallery.py` into a framework-agnostic core (example
  collection, category registry, SVG poster, shared `_static` paths) reused by
  both the new Sphinx extension and the thumbnail renderer.
- Added `docs/_ext/genomespy_gallery.py`: on `builder-inited` it writes the spec
  JSON, SVG posters, per-example detail pages, and the category-grouped gallery
  index; it also registers a `genomespy-minigallery` directive that emits the
  Altair-style image-tile showcase (172x100 cover tiles linking to detail pages)
  for the landing page.
- Rebuilt the landing page (`docs/index.md`, MyST) as hero + minigallery
  showcase + sphinx-design intro cards; converted user-guide Material
  admonitions to MyST; replaced the mkdocstrings API page with Sphinx
  autodoc/napoleon (`docs/api.md`). Re-created the brand CSS/JS under
  `docs/_static/` and branded the pydata theme with the genomespy.app palette
  and Lato/Special Elite fonts.
- Fixed the detail-embed spec path for Sphinx's flat page layout
  (`../_static/specs/<name>.json` from `/gallery/<name>.html`); verified 200 over
  a local HTTP serve for landing/gallery/detail/spec/thumbnail.
- Updated `.github/workflows/docs.yml` to build with
  `sphinx-build -b html -W --keep-going docs docs/_build/html` and publish that
  directory to Pages on release; the Playwright thumbnail step is unchanged.
- Validation: `uv run sphinx-build -b html -W --keep-going docs docs/_build/html`
  (exit 0, no warnings), `uv sync --locked --group docs`, `uv run ruff check
  tools/ docs/_ext docs/conf.py tests/test_docs_gallery.py`, `uv run mypy src/`,
  and `uv run pytest tests/ -q` (53 passed).

## 2026-07-07 - Expand gallery: QQ, volcano, admixture, genome tracks

- Added four gallery examples alongside Manhattan, each authored as a
  `docs/examples/*.py` module with data wrangling in a function and the
  visualization as the main body: `qq_plot` (GWAS), `volcano_plot` (RNA-seq),
  `admixture` (Populations), and `genome_tracks` (Genome tracks). The gallery
  now populates four categories.
- Fixed a real library bug found while authoring the interval/stacked examples:
  `_normalize_channel` inferred a `type` for secondary positional channels
  (`x2`/`y2`) and only stripped it when the type was `locus`. GenomeSpy's schema
  forbids a `type` on `x2`/`y2` entirely, so plain quantitative secondary
  channels (coverage bars, stacked admixture, the QQ diagonal `link`) never
  validated. Now `x2`/`y2` never receive an inferred type and any provided type
  is stripped; added a regression test in `tests/test_chart.py`.
- Noted two GenomeSpy grammar specifics while building examples: the text mark
  uses `size` (not `fontSize`), and diagonals are drawn with the `link` mark
  using `x`/`y`/`x2`/`y2`.
- Validation: `uv run pytest tests/ -q` (58 passed, incl. 4 new example specs
  and the x2/y2 regression), `uv run ruff check`, `uv run mypy src/`, and
  `uv run sphinx-build -b html -W --keep-going docs docs/_build/html` (exit 0,
  no warnings) — five detail pages, four category sections, and five showcase
  tiles rendered.
- Follow-up: the four new plots rendered as static charts because GenomeSpy only
  makes genomic/locus scales zoomable by default (Manhattan zooms via its locus
  x-scale). Enabled interaction by setting `zoom=True` on the continuous
  position scales of QQ, volcano, admixture, and the genome-tracks example.
  Hover tooltips are GenomeSpy's default. Re-validated specs and rebuilt
  (`zoom: true` present in every new spec).

## 2026-07-07 - Fix docs tooltips (iframe) and add real-data example

- Tooltips worked in the anywidget notebook but not on the Sphinx pages. Ruled
  out (via the built theme CSS) a `.tooltip` class collision and transformed/
  contained ancestors. Headless Chromium can't verify GenomeSpy tooltips because
  its WebGL pick-buffer readback is unreliable, so this was diagnosed by
  isolation: the chart works in a clean document (notebook) and fails only when
  embedded in the themed page.
- Fix: render each live chart inside an `<iframe srcdoc>` — a fresh document
  isolated from the theme's CSS, matching the notebook environment. The srcdoc
  imports the pinned bundle and fetches the spec (its base URL is the parent
  page, so the relative spec path resolves). Removed the now-unused
  `gallery-hydrate.js` and the flex/padded live container; frame styling moved to
  the iframe. Verified in a real browser via Playwright that the iframe renders
  the GenomeSpy canvas with no errors (tooltip visibility itself needs a real
  GPU browser to confirm).
- Real data: the `tmp/dataviz-genomicsdata` parquet files are deterministically
  generated synthetic data with no data license, so they are not more "real"
  than ours. The clean, license-safe, CI-safe source is GenomeSpy's own hosted
  sample data referenced by URL. Added `docs/examples/copy_number.py` — a real
  ASCAT allele-specific copy-number profile (sample S96) streamed from
  `data.genomespy.app`, with a new "Copy number" gallery category. Confirmed the
  chart fetches and renders the real data in the browser.
- Validation: `uv run pytest tests/test_docs_gallery.py -q` (7 examples validate),
  `uv run ruff check`, and `uv run sphinx-build -b html -W --keep-going`
  (exit 0, no warnings).
- Confirmed the `tmp/` genomics repos are all simulated (`np.random` builders;
  ggwas is documented "simulated"), so real data must come from external
  sources. Real stat-plot CSVs are not cleanly available (airway needs R/DESeq2;
  1000G admixture has data-use agreements), so pursuing real data genomic-first.
- Phase 1 (real genomic examples): added `docs/examples/allele_specific.py` — a
  real per-SNP ASCAT logR/BAF two-track view for sample S96 (~10k points),
  streamed from `data.genomespy.app`, pairing with the copy-number segments.
  Gene/cytoband/cCRE tracks are deferred: those hosted files are raw gzip /
  bigBed / bigWig needing lazy-indexed loaders our API does not yet wrap.
- Phase 2 (pending): source and license-verify real tabular datasets for the
  synthetic stat plots (volcano, admixture, QQ, Manhattan) before vendoring.
- Validation: `uv run pytest tests/test_docs_gallery.py -q` (8 examples validate),
  `uv run ruff check`, and `uv run sphinx-build -b html -W --keep-going`
  (exit 0), with a real-browser check that both copy-number examples fetch and
  render the hosted data.

## 2026-07-07 - Phase 2: real GWAS data for Manhattan, volcano, QQ

- Traced the Dash Bio Manhattan/volcano data (`git.io/manhattan_data.csv`,
  `volcano_data1.csv`) to `plotly/datasets` (MIT), and upstream to the `HapMap`
  dataset in the `manhattanly` R package (MIT): "Subset of HapMap data with
  simulated GWAS results" — real HapMap coordinates/rs-IDs/gene names with
  simulated association statistics. Checked qqman too, but its data is also
  simulated and it is GPL-3, so preferred the MIT HapMap data.
- Vendored `docs/_static/data/hapmap_gwas.csv` (14,412 rows, MIT) with a
  provenance + license notice in `docs/_static/data/README.md`, and rewrote the
  Manhattan, volcano, and QQ examples to load it (mapping HapMap chromosome 23
  to X; GenomeSpy assemblies take bare chromosome names as in its own ASCAT
  data). Verified all three render the real data in a real browser.
- Moved the volcano to the GWAS category (it now plots real GWAS effect size vs
  significance). Labelled the two remaining synthetic examples (genome tracks,
  admixture) as synthetic in their descriptions. Gallery is now 7 examples:
  5 real-data (3 HapMap GWAS + 2 hosted ASCAT copy-number), 2 synthetic.
- Dash Bio's other datasets (needle-plot PIK3CA, oncoprint, alignment p53) are
  MIT and remain candidates for future example types (mutation needle plot,
  oncoprint, MSA) that need new chart patterns.
- Validation: `uv run pytest tests/ -q` (60 passed), `uv run ruff check`, and
  `uv run sphinx-build -b html -W --keep-going docs docs/_build/html` (exit 0),
  with real-browser render checks for Manhattan, volcano, and QQ.

## 2026-07-07 - Visual fixes: axis ranges, iframe sizing, QQ diagonal

- Installed Playwright + Chromium locally, which finally allows screenshotting
  charts to actually see them (headless still can't verify hover tooltips, but
  render/layout is faithful).
- Volcano "looked silly" because the shared x/y scales collapsed onto the tiny
  cutoff-line datasets, clipping every point to the axis edges. Fixed by setting
  explicit data-derived `domain`s on the shared x/y scales in the volcano and
  Manhattan examples (and widened the effect cutoff to 0.5). Volcano now shows a
  proper shape with full ranges and colored significant hits.
- Cleaned up concatenated axis titles (e.g. "threshold, −log10 p, neglog") by
  giving every layer the same axis title on the shared channel.
- QQ diagonal was drawn with the `link` mark, which renders an arc; switched to
  `mark_link(..., linkShape="line")` for a straight identity line.
- Iframe empty-space fix: GenomeSpy sizes locus/genomic charts responsively but
  linear charts to their spec size. The srcdoc now uses a fixed-width container
  (so locus charts don't collapse), measures the rendered canvas, and posts its
  size to the parent (`gallery-resize.js`) which shrink-wraps the iframe. Charts
  now fit their iframe with no empty space; verified across all 7 examples.
- Validation: `uv run pytest tests/ -q` (60 passed), `uv run ruff check`,
  `uv run mypy src/`, and `uv run sphinx-build -b html -W --keep-going` (exit 0),
  with screenshots of Manhattan, QQ, volcano, and the allele-specific tracks.

## 2026-07-04 - Handwritten chart typing alignment

- Tightened the handwritten `Chart` / composition API annotations so they align
  better with the generated helper types added in the schema layer.
- `Chart.__init__` now accepts `EncodingKwds` for raw encoding mappings, and
  `resolve_axis` / `resolve_scale` / `resolve_legend` now use
  `GenomeAxisKwds`, `ScaleKwds`, and `LegendKwds` instead of broad
  `dict[str, Any]` annotations.
- This was a typing-alignment pass only; runtime behavior was kept unchanged so
  the ergonomic layer still normalizes schema wrappers and raw mappings into
  plain spec dictionaries before validation.
- Validation: `UV_CACHE_DIR=.uv-cache uv run ruff format src/genome_spy/chart.py
  tests/test_chart.py`, `UV_CACHE_DIR=.uv-cache uv run ruff check
  src/genome_spy/chart.py tests/test_chart.py src/genome_spy/schema`,
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`, and
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_chart.py
  tests/test_generated_schema_package.py -q` (38 passed).

## 2026-07-04 - Final helper-object typing sweep

- Extended the selective `_kwds.py` helper generation to cover additional
  repeated fixed-shape helper objects that still appeared widely as
  wrapper-or-`dict[str, Any]` unions, including `HandledTooltip`,
  `CompareParams`, `SchemeParams`, `ScaleInterpolateParams`, `ZoomParams`,
  binding configs, and several mark/config helper shapes.
- Deliberately left `Parse` broad because the schema models it as an
  open-ended mapping with `additionalProperties` rather than a fixed object
  shape, so generating a closed `TypedDict` there would create fake precision.
- Added a generated-package regression test that exercises the new helper types
  through actual wrapper slots (`PointProps.tooltip`, `ViewOpacityDef.sort`,
  and `Scale.scheme`) instead of only embedding them inside arbitrary raw dicts.
- Validation: `UV_CACHE_DIR=.uv-cache uv run ruff format src/genome_spy/schema
  tools/schemapi/codegen.py tests/test_generated_schema_package.py`,
  `UV_CACHE_DIR=.uv-cache uv run ruff check src/genome_spy/schema
  tools/schemapi/codegen.py tests/test_generated_schema_package.py
  src/genome_spy/chart.py`, `UV_CACHE_DIR=.uv-cache uv run mypy src/`, and
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/ -q` (51 passed).

## 2026-07-04 - Docs site plan and MkDocs scaffold

- Chose the documentation stack and gallery direction with the maintainer:
  MkDocs + Material for MkDocs (deliberately diverging from Altair's Sphinx to
  avoid a look-alike), a hybrid gallery where each card paints a static poster
  then hydrates into a live interactive GenomeSpy chart on hover/focus, and a
  deploy-on-release pipeline. Recorded in `plans/docs_site_plan.md`.
- Established the single-source-of-truth example pattern: each example is a
  runnable module under `docs/examples/*.py` defining `chart` (+ optional
  `META`). `tools/docs_gallery.py` imports it, serializes `chart.to_dict()`,
  and emits the spec JSON, a branded SVG poster, the card grid, and a per-example
  detail page (live chart + source + spec). A MkDocs `on_pre_build` hook
  (`tools/mkdocs_hooks.py`) runs it on every build/serve; generated
  `docs/gallery/` and `docs/assets/generated/` are git-ignored.
- Scaffolded the site: `mkdocs.yml`, a genome-spy-branded theme
  (`docs/stylesheets/genomespy.css`), the hover/scroll hydrate harness
  (`docs/javascripts/gallery-hydrate.js`, lazily importing the pinned
  `@genome-spy/core` CDN bundle via the same `embed` entrypoint as the widget),
  landing/getting-started/user-guide/about pages, and an mkdocstrings API
  reference. Wired the Manhattan example end to end.
- Added `.github/workflows/docs.yml`: build-check on push to `main`, and
  `mkdocs build --strict` + GitHub Pages deploy on `release: published` /
  manual dispatch, so public docs rebuild on each release. Added a `docs`
  dependency group to `pyproject.toml`.
- Static posters are stage-1 SVGs so the site builds offline; a Playwright
  screenshot stage is planned as a follow-up (no pure-Python renderer exists for
  GenomeSpy's WebGL output).
- Validation: `uv run python tools/docs_gallery.py` (1 example),
  `uv run mkdocs build --strict` (exit 0),
  `uv run ruff check tools/docs_gallery.py tools/mkdocs_hooks.py
  tests/test_docs_gallery.py docs/examples/manhattan_plot.py`, and
  `uv run pytest tests/ -q` (53 passed, incl. 2 new gallery tests).

## 2026-07-04 - Docs redesign to genomespy.app style + static gallery

- Reworked the docs after maintainer feedback (landing looked poor, live-in-card
  chart was mis-sized, specs too long to show, and the detail chart failed to
  open from a card).
- Pulled genomespy.app's actual design system from its CSS and matched it: Lato
  body, Special Elite wordmark, brand blues (`#3e8cb6` / `#3797ce` / hover ring
  `#7fbbdd`), white surface with light-grey sections. Set a single light Material
  scheme `genomespy`; dropped the dark toggle for now.
- Switched the gallery from live hover-hydrated cards to **static image cards**
  (like Altair, Vega-Lite, and genomespy.app): a screenshot thumbnail that lifts
  and shows the brand ring on hover, with a subtle image zoom. The full
  interactive chart now lives only on the detail page. This fixed the mis-sized
  in-card render.
- Fixed the detail-page embed bug: cards open at `/gallery/<name>/`, so the raw
  HTML `data-spec` attribute needs `../../assets/...` (browser-resolved), while
  the Markdown download link needs `../assets/...` (MkDocs source-resolved).
  Verified over HTTP that the detail page and its spec both return 200.
- Removed the giant inline spec dump from detail pages; replaced it with a
  "Download the generated GenomeSpy spec" link.
- Made the **landing page the gallery**: a genomespy-style hero followed by the
  category card decks, included via a generated `home_deck.html` snippet so the
  hero prose stays hand-written and the cards stay generated.
- Added category taxonomy with per-section blurbs (GWAS, RNA-seq, Genome tracks,
  Populations, Basics) in `tools/docs_gallery.py`.
- Real thumbnails: added `tools/render_thumbnails.py` (Playwright headless
  screenshot of each live chart) wired as a best-effort CI step in
  `docs.yml`; when no PNG exists the generator falls back to an improved SVG
  poster, so local/offline builds still work. Playwright could not be installed
  in this session (PyPI unreachable), so thumbnails are SVG fallbacks here.
- Validation: `uv run mkdocs build --strict` (exit 0), local HTTP serve returned
  200 for landing/detail/spec/thumbnail, `uv run ruff check` on the changed
  tools/tests, and `uv run pytest tests/ -q` (53 passed).

## 2026-07-03 - Gallery data and plot planning

- Reviewed `ggwas`, `dataviz-genomicsdata`, `gos`, and `gosling.js` as
  gallery-design references, with `ggwas` guiding GWAS plot structure and
  `dataviz-genomicsdata` providing the clearest small deterministic datasets.
- Added `plans/gallery_data_plot_plan.md` as a concrete brief for future
  gallery work: it maps candidate plots to local data sources, expected visual
  appearance, likely GenomeSpy spec ingredients, wrangling needs, and the split
  between mechanical validation and user-led visual QA.
- Chose an explicit first gallery slice and implementation order:
  Manhattan, QQ, peak/gene locus tracks, admixture, then volcano.

## 2026-07-03 - First gallery notebook: Manhattan

- Added `notebooks/gallery_manhattan.ipynb` as the first dedicated gallery
  example notebook.
- Used a deterministic synthetic GWAS table shaped after
  `dataviz-genomicsdata`'s `build_gwas()` helper, but rendered it with a
  GenomeSpy-native locus x-scale and `GenomeAxis` chromosome styling rather
  than a manually accumulated x-coordinate.
- Layered subtle significance rules and highlighted top hits, while deferring
  text labels for now because the first attempt at a locus-based text layer did
  not validate against GenomeSpy core's schema.

## 2026-07-03 - Manhattan notebook encoding cleanup

- Refined `notebooks/gallery_manhattan.ipynb` after visual QA feedback that
  the layered color encodings were ambiguous.
- Kept odd/even chromosome grouping as the only point-color meaning, changed
  significance thresholds into fixed-color rule annotations, and made top hits
  stand out via larger outlined points instead of a separate purple category.
- Re-executed the notebook in place and reran `uv run pytest tests/test_notebook.py -q`
  (8 passed).

## 2026-07-03 - Planned Altair-style schema typing upgrade

- Confirmed that the current schema generator emits constructor arguments as
  `Any = Undefined` by design; this is a generator limitation, not an upstream
  GenomeSpy requirement.
- Compared against the cloned Altair wrapper and found that Altair's generated
  schema classes use schema-derived constructor unions plus supporting aliases
  and `TypedDict` helpers rather than blanket `Any`.
- Updated planning docs so a new typed-schema-generation workstream is now
  explicit: start with scalar/enum/ref/array/object constructor typing, then
  add shared aliases, selective `TypedDict` helpers, and matching nested-setter
  typing.

## 2026-07-03 - Typed schema generation T1

- Implemented the first schema-derived typing slice in the code generator:
  generated constructor arguments now map common JSON Schema forms to Python
  annotations instead of defaulting wholesale to `Any`.
- The new generated coverage includes primitives, enum literals, referenced
  wrapper classes plus raw dict inputs, arrays as `Sequence[...]`, and explicit
  `None` when the schema allows null; `UndefinedType` is now part of generated
  constructor annotations so omission remains type-checker-visible.
- Regenerated the checked-in schema package from the packaged GenomeSpy schema
  and updated codegen tests to assert representative typed output rather than
  expecting `Any = Undefined`.
- Adjusted the handwritten chart layer with a few narrow `cast(Any, ...)`
  boundary calls where the ergonomic API intentionally accepts broader values
  than the current first-pass generated signatures can yet express cleanly.
- Validation: `uv run pytest tests/ -q` (49 passed), `uv run ruff check
  tools/schemapi/codegen.py tests/test_schema_codegen.py src/genome_spy/schema`,
  and `uv run mypy src/` all passed.

## 2026-07-03 - Typed schema generation T1 refinement

- Refined `$ref` typing so the generator now distinguishes object-like
  references from scalar-like references instead of treating every ref as a
  wrapper-class-plus-dict input.
- Enum and scalar union refs such as alignment, orientation, and mark-type-like
  values now inline to Python literal/primitive unions in generated
  constructors, while true object refs still accept generated wrapper instances
  and raw dicts.
- Aligned generated `with_...` setter typing with the same rule: only object-
  like refs get wrapper-aware nested setter signatures; scalar-like refs stay
  plain value setters.
- Validation: `uv run ruff check tools/schemapi/codegen.py
  tests/test_schema_codegen.py src/genome_spy/schema src/genome_spy/chart.py`,
  `uv run mypy src/`, and `uv run pytest tests/ -q` (49 passed).

## 2026-07-03 - Typed schema generation T2 alias layer

- Added a generated `src/genome_spy/schema/_typing.py` module for shared
  scalar-like schema aliases such as `Align_T`, `AxisOrient_T`, `MarkType_T`,
  `LegendOrient_T`, and related repeated enum/value families.
- Updated generated `core.py` to import and use only the aliases that actually
  appear in generated constructor signatures, reducing repetition in large
  classes like `GenomeAxis`, `AxisConfig`, and top-level spec types.
- Tightened alias eligibility so `_typing.py` only contains genuinely
  scalar-like reusable aliases; wrapper-class-dependent unions are left in
  `core.py` to avoid circular imports and confusing cross-module coupling.
- Ordered generated aliases by dependency so alias-on-alias references such as
  positional-channel families remain valid at runtime and under mypy.
- Validation: `uv run ruff check tools/schemapi/codegen.py
  tools/generate_schema_wrapper.py tests/test_schema_codegen.py
  src/genome_spy/schema src/genome_spy/chart.py`, `uv run mypy src/`, and
  `uv run pytest tests/ -q` (49 passed).

## 2026-07-03 - Typed schema alias simplification

- Simplified the generated alias layer by teaching the code generator that a
  schema branch with `const` should contribute only its literal value rather
  than both the literal and the broader primitive type.
- This removed several noisy broad-type redundancies from `_typing.py`, for
  example cleaning `FontWeight_T`, `DomEventType_T`, and
  `ChannelWithScale_T` so they better reflect the actual schema intent.
- Regenerated the schema package after the simplification; runtime behavior is
  unchanged, but the generated type surface is clearer and closer to the
  Altair-style quality bar.
- Validation: `uv run ruff check tools/schemapi/codegen.py
  tools/generate_schema_wrapper.py tests/test_schema_codegen.py
  src/genome_spy/schema src/genome_spy/chart.py`, `uv run mypy src/`, and
  `uv run pytest tests/ -q` (49 passed).

## 2026-07-03 - Typed fluent setters for scalar properties

- Extended the schema-derived typing work from constructors into generated
  `with_...` fluent setters for scalar and alias-backed properties instead of
  leaving those setter arguments as blanket `Any`.
- Regenerated setters now preserve the same value-family information already
  present in constructor signatures, for example `Align_T`, `AxisOrient_T`,
  `FontWeight_T`, and `Sequence[...]` cases.
- Object-like nested properties continue to use the wrapper-aware setter path,
  while plain scalar/value properties now expose clearer setter signatures in
  the checked-in generated code.
- Validation: `uv run ruff check tools/schemapi/codegen.py
  tools/generate_schema_wrapper.py tests/test_schema_codegen.py
  src/genome_spy/schema src/genome_spy/chart.py`, `uv run mypy src/`, and
  `uv run pytest tests/ -q` (49 passed).

## 2026-07-03 - Codegen structure cleanup

- Reviewed `tools/schemapi/codegen.py` against Altair's generator layout and
  found that our implementation matched the broad direction but had become too
  ad hoc internally by mixing schema inspection, type inference, alias
  selection, and source rendering in one flat helper layer.
- Refactored the generator around a small internal `SchemaAnalyzer` plus
  `PropertySpec` records so `SchemaWrapperGenerator` now reads more like an
  orchestrator while schema traversal and annotation derivation live behind a
  clearer boundary.
- Kept the public generator API and generated outputs stable; this was an
  internal maintainability pass to make the next typing/codegen work easier to
  reason about.
- Validation: `UV_CACHE_DIR=.uv-cache uv run ruff format
  tools/schemapi/codegen.py tests/test_schema_codegen.py`,
  `UV_CACHE_DIR=.uv-cache uv run ruff check tools/schemapi/codegen.py
  tests/test_schema_codegen.py tools/generate_schema_wrapper.py`,
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_schema_codegen.py -q`
  (2 passed), and `UV_CACHE_DIR=.uv-cache uv run mypy src/`.

## 2026-07-03 - Selective schema-backed TypedDict helpers

- Added a generated `src/genome_spy/schema/_kwds.py` module with selective
  `TypedDict` helpers for config-heavy object shapes such as axis, scale,
  legend, title, encoding, and top-level config-like mappings.
- Updated schema-derived annotations so selected object refs now accept
  wrapper instances plus schema-backed raw mapping helpers like `AxisKwds` and
  `ScaleKwds` instead of always falling back to plain `dict[str, Any]`.
- Kept the handwritten chart layer aligned by preserving a narrow `cast(Any, …)`
  boundary where the ergonomic API intentionally assembles looser encoding
  dicts before handing them to the generated schema layer.
- Regenerated the checked-in schema package against the pinned
  `@genome-spy/core` version from `pyproject.toml` so `SCHEMA_VERSION`
  remains `0.80.0`.
- Validation: `UV_CACHE_DIR=.uv-cache uv run ruff format src/genome_spy/schema
  src/genome_spy/chart.py tools/schemapi/codegen.py
  tools/generate_schema_wrapper.py tests/test_schema_codegen.py`,
  `UV_CACHE_DIR=.uv-cache uv run ruff check src/genome_spy/schema
  src/genome_spy/chart.py tools/schemapi/codegen.py
  tools/generate_schema_wrapper.py tests/test_schema_codegen.py`,
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`, and
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/ -q` (49 passed).

## 2026-07-03 - Composition mapping helper typing

- Extended the generated helper layer to cover inline composition mappings that
  were still exposed as plain `dict[str, Any]` in top-level specs: `axes`,
  `legends`, `resolve`, and `scales`.
- Added selective anonymous helper types in `schema/_kwds.py` such as
  `AxesKwds`, `LegendsKwds`, `ScalesKwds`, `ResolveKwds`, and the nested
  `AxisResolveKwds` / `LegendResolveKwds` / `ScaleResolveKwds` structures.
- Kept `templates` intentionally broad because the schema models it as an
  open string-keyed mapping of named `ViewSpec` values rather than a fixed
  shape.
- Updated generated schema annotations to use these helpers where the inline
  schema structure is stable, and added a runtime regression test that
  exercises composition-resolution mappings through the public generated API.
- Validation: `UV_CACHE_DIR=.uv-cache uv run ruff format src/genome_spy/schema
  src/genome_spy/chart.py tools/schemapi/codegen.py
  tests/test_generated_schema_package.py tests/test_schema_codegen.py`,
  `UV_CACHE_DIR=.uv-cache uv run ruff check src/genome_spy/schema
  src/genome_spy/chart.py tools/schemapi/codegen.py
  tools/generate_schema_wrapper.py tests/test_schema_codegen.py
  tests/test_generated_schema_package.py`,
  `UV_CACHE_DIR=.uv-cache uv run mypy src/`, and
  `UV_CACHE_DIR=.uv-cache uv run pytest tests/ -q` (50 passed).

## 2026-07-02 - Future documentation gallery references

- Reviewed the newly cloned `tmp/dataviz-genomicsdata` and `tmp/ggwas`
  repositories as visual and example-design references.
- Added both repositories to the reference index and documented a phased
  GenomeSpy example-gallery candidate list in `plans/example_gallery.md`.
- Decided to defer gallery production until the schema-backed API, composition,
  and notebook rendering path are stable; reference examples should not drive
  speculative core convenience APIs.
- Validation: `git diff --check`, `uv run pytest tests/ -q` (28 passed),
  `uv run ruff check .`, and `uv run mypy src/` all passed; no runtime code was
  changed in this session.

## 2026-07-02 - Schema-backed chart bridge and dataset inventory

- Reviewed `tmp/dataviz-genomicsdata/genomics_course/genomics_course/data/` and
  cataloged its deterministic builders and compact genomics datasets as future
  gallery references; no clear repository-level license was found, so the data
  was not copied into this package.
- Rechecked Altair's layering and confirmed its handwritten `Chart` subclasses
  a generated unit-spec class; retained that as the target architecture.
- Updated code generation to emit `MARK_TYPES` from GenomeSpy's schema and
  changed the handwritten chart layer to consume it and serialize unit-spec
  fields through generated `UnitSpec`.
- Normalized the copied schema file's trailing newline for deterministic
  regeneration and added focused codegen/package tests.
- Validated with `uv run ruff format .`, `uv run pytest tests/ -q` (28 passed),
  `uv run ruff check .`, `uv run mypy src/`, and `git diff --check`.
- Follow-up: generate mark mixins and add schema validation/copy semantics
  before changing `Chart` to directly subclass `UnitSpec`.

## 2026-07-02 - Generated mark mixins and schema validation

- Added generated `schema/mixins.py` mark methods derived from the upstream
  `MarkType` enum and retained `mark_circle()` as a handwritten Python alias.
- Added deep/shallow copy semantics and default-on JSON Schema validation to
  generated wrappers, with `SchemaValidationError` context and
  `validate=False` escape hatches.
- Changed public charts to validate the fully assembled specification once
  through generated `Root`; composition children skip redundant validation.
- Added `jsonschema` as a direct runtime dependency and added focused tests for
  generation, invalid marks, validation opt-out, and deep-copy isolation.
- Validated with `uv run ruff format .`, `uv run pytest tests/ -q` (31 passed),
  `uv run ruff check .`, `uv run mypy src/`, and `git diff --check`.
- Follow-up: migrate the frozen-dataclass chart builder to schema-object copy
  semantics before direct `UnitSpec` inheritance; generate channels next.

## 2026-07-02 - Dead-code cleanup

- Removed the unreachable missing-`anywidget` fallback and its stale typing
  suppression because `anywidget` is a required runtime dependency.
- Removed reference-source tests that depended on ignored local `tmp/` clones
  and therefore could not run in a clean checkout or CI; package behavior tests
  remain.
- Removed the unused duplicate `tools/schemapi/schemapi.py`; the active schema
  runtime is maintained only in `src/genome_spy/schemapi.py`.
- Validated with `uv run ruff format .`, `uv run pytest tests/ -q` (28 passed),
  `uv run ruff check .`, `uv run mypy src/`, `uv build`, and
  `git diff --check`.

## 2026-07-02 - Version-pinned notebook runtime

- Derived the default CDN schema and GenomeSpy JavaScript bundle URLs from the
  generated `SCHEMA_VERSION` instead of loading the latest unversioned package.
- Added regression coverage ensuring both runtime URLs match the generated
  schema package version.
- Validated with `uv run ruff format .`, `uv run pytest tests/ -q` (29 passed),
  `uv run ruff check .`, `uv run mypy src/`, and `git diff --check`.

## 2026-07-02 - Generated encoding channels

- Added deterministic generation of `schema/channels.py` from every property
  in GenomeSpy core's `Encoding` schema (23 channels in version 0.78.0).
- Replaced handwritten public `X`, `Y`, `X2`, `Y2`, `Color`, `Size`, `Text`, and
  `Opacity` constructors with generated wrapper classes while preserving
  shorthand parsing, positional channel inference, fluent methods, `value()`,
  and `Locus()` behavior.
- Kept direct `Chart`/`UnitSpec` inheritance deferred as a separate state-model
  migration: the current chart is an immutable dataclass, while generated
  wrappers store mutable `_kwds`; nominal multiple inheritance would not
  provide real schema-backed behavior.
- Validated with `uv run ruff format .`, `uv run pytest tests/ -q` (31 passed),
  `uv run ruff check .`, `uv run mypy src/`, `uv build`, and execution of all
  four notebooks into temporary outputs.

## 2026-07-02 - Direct UnitSpec chart inheritance

- Verified Altair's current structure: handwritten top-level, encoding, and
  mark mixins subclass a generated top-level unit-spec class, and builder
  methods return shallow schema-object copies.
- Refactored `Chart` to directly inherit generated `UnitSpec`, store schema
  properties in `_kwds`, and preserve immutable-style `mark`, `encode`,
  `properties`, and transform chaining through schema-backed copies.
- Split shared top-level behavior from composition storage; layer and concat
  containers remain small immutable dataclasses while accepting schema-backed
  `Chart` children through the common top-level interface.
- Added generated-property attribute access and regression coverage for direct
  inheritance, non-mutation, and preservation of custom schema URLs.
- Validated with `uv run ruff format .`, `uv run pytest tests/ -q` (32 passed),
  `uv run ruff check .`, `uv run mypy src/`, `uv build`, and execution of all
  four notebooks into temporary outputs.

## 2026-07-02 - GenomeSpy core 0.80.0 upgrade

- Verified from the npm registry that `@genome-spy/core` 0.80.0 is current,
  updated the project pin from 0.78.0, and regenerated the packaged schema and
  Python wrappers twice to confirm deterministic output after formatting.
- Reviewed the schema delta: encoding channels and mark types are unchanged;
  0.80.0 adds legend, ruler, URL-template, selection-extent, and axis-placement
  definitions and removes `ViewBackgroundProps`.
- Updated the CI freshness job to format generated Python before comparing it
  with committed artifacts, matching the maintainer workflow.
- Added regression coverage for the pinned generated schema version.
- Validated with `uv run pytest tests/ -q` (32 passed), `uv run ruff check .`,
  `uv run mypy src/`, `uv build`, `git diff --check`, and execution of all four
  notebooks from temporary copies.

## 2026-07-02 - Planned schema-release automation

- Added a roadmap item to automate detection of new `@genome-spy/core`
  releases and to open a regeneration PR after running the existing maintainer
  workflow.
- Explicitly deferred implementation of that automation so it does not get
  tangled with the active wrapper and notebook workstream.

## 2026-07-02 - Schema-derived channel nested setters

- Added a roadmap-aligned next slice after the `UnitSpec` migration: generated
  channel classes now expose schema-derived `axis`, `scale`, and `legend`
  helpers only when the upstream `Encoding` schema allows those nested
  properties.
- Taught the shared channel runtime to accept nested schema wrapper instances,
  raw mappings, or `None` for those generated setters while preserving the
  existing keyword-merge behavior.
- Regenerated `src/genome_spy/schema/channels.py` from the packaged GenomeSpy
  schema and added regression coverage for wrapper-based nested setters and the
  schema-derived generated surface.
- Validated with `uv run pytest tests/ -q` (35 passed), `uv run ruff check .`,

## 2026-07-02 - Resolved constructor signatures for generated wrappers

- Taught the schema generator to resolve reachable properties through `$ref`,
  `anyOf`, `oneOf`, and `allOf` branches when building generated wrapper
  constructor signatures.
- Regenerated `src/genome_spy/schema/core.py` so wrappers like `ColorDef` and
  `PositionDef` expose useful keyword parameters instead of falling back to
  bare `**kwds` constructors.
- Added regression coverage for both the generated signature surface and the
  resulting serialized wrapper output.
- Validated with `uv run pytest tests/test_generated_schema_package.py
  tests/test_chart.py -q` (31 passed), `uv run pytest tests/ -q` (36 passed),
  `uv run ruff check .`, `uv run mypy src/`, `uv build`, `git diff --check`,
  and execution of all four example notebooks before restoring validation-only
  output changes.

## 2026-07-02 - Fluent generated helper setters

- Added immutable schema-derived `with_<property>(...)` methods to generated
  schema wrappers so commonly used helper objects such as `Scale`, `Legend`,
  and `GenomeAxis` can be built fluently without dropping down to raw dicts.
- Reused the generated runtime for nested-property updates: helper setters now
  accept wrapper instances, raw mappings, or keyword merges for nested object
  properties.
- Added regression coverage for direct helper-object chaining and nested helper
  merges from wrappers such as `ColorDef` and `PositionDef`.
- Validated with `uv run pytest tests/test_generated_schema_package.py -q`
  (9 passed), `uv run ruff check .`, `uv run mypy src/`, and `git diff
  --check`.

## 2026-07-02 - Schema-backed composition containers

- Replaced the handwritten dataclass composition containers with schema-backed
  `LayerSpec`, `HConcatSpec`, `VConcatSpec`, and `ConcatSpec` subclasses while
  preserving the existing public operators and top-level serialization helpers.
- Kept composition child serialization explicit so nested charts still omit
  `$schema` and skip redundant child validation before the full spec validates
  once through generated `Root`.
- Added regression coverage confirming that public composition charts now
  directly inherit the generated schema classes and preserve custom schema URLs
  across immutable-style copy operations.
- Validated with `uv run pytest tests/test_chart.py
  tests/test_generated_schema_package.py -q` (35 passed), `uv run pytest
  tests/ -q` (40 passed), `uv run ruff check .`, `uv run mypy src/`,
  `uv build`, and `git diff --check`.

## 2026-07-02 - Composition resolution ergonomics

- Added small handwritten composition helpers on top of the schema-backed
  containers: `resolve_scale(...)`, `resolve_axis(...)`, and
  `resolve_legend(...)`.
- Mapped those helpers onto GenomeSpy's own top-level composition properties
  (`scales`, `axes`, and `legends`) rather than Vega-Lite's single `resolve`
  object, while still following Altair's user-facing pattern.
- Added regression coverage for mixed wrapper/dict resolution inputs on a
  composed chart.
- Validated with `uv run pytest tests/test_chart.py -q` (27 passed),
  `uv run pytest tests/ -q` (41 passed), `uv run ruff check .`,
  `uv run mypy src/`, `uv build`, and `git diff --check`.

## 2026-07-02 - Composition resolution example notebook

- Added `notebooks/composition_resolution_example.ipynb` with small dummy data
  and a side-by-side composition that demonstrates `resolve_scale(...)`,
  `resolve_axis(...)`, and `resolve_legend(...)`.
- Relaxed the notebook smoke test so it validates all example notebooks
  without assuming an exact final `chart` display cell shape.
- Validated with `uv run pytest tests/test_notebook.py -q` (5 passed),
  `uv run ruff check tests/test_notebook.py`, and execution of the new notebook
  via `uv run jupyter nbconvert --to notebook --execute --inplace`.
- Follow-up: adjusted the notebook from `hconcat` to `layer` after confirming
  GenomeSpy rejects view-level `scales.x` on a concat subtree when the config
  maps to multiple visible scale resolutions.
- Follow-up: split the examples into two notebooks instead: kept
  `composition_resolution_example.ipynb` as a valid concat demo and added
  `layered_resolution_helpers.ipynb` as the dedicated resolution-helper demo.
- Follow-up: documented the practical example-writing rule that GenomeSpy
  rejects mixing view-level `scales`, `axes`, or `legends` with conflicting
  encoding-level channel configs in the same resolution. In practice, notebook
  examples should avoid view-level overrides on channels where our public
  channel helpers already inject encoding-level config, such as the default
  `Y(...).scale(...)` behavior.

## 2026-07-02 - First genomics-native example notebook

- Added `notebooks/genomic_interval_tracks.ipynb`, a small synthetic genomics
  example that uses locus encodings, secondary `x2`, and vertical composition
  to stack an interval track above a variant-signal track.
- Added regression coverage for locus intervals so the handwritten
  `gs.Locus(...)` helper plus secondary `x2` remains validated through schema
  serialization.
- Kept the data synthetic on purpose: the gallery-reference repositories in
  `tmp/` remain design inputs, but this package still avoids silently vendoring
  their datasets before license/provenance choices are explicit.
