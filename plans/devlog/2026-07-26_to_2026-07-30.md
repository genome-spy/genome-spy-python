# Devlog archive: 2026-07-26 to 2026-07-30

## 2026-07-30 - Added a scrollable LUAD oncoprint viewport

- Applied GenomeSpy's concat viewport pattern to the outer LUAD `hconcat` with
  `viewportHeight="container"`, keeping the matrix and right-side summaries
  synchronized while scrolling vertically.
- Reduced the gallery embed height to 720 px so the oncoprint's natural stacked
  content exceeds the visible viewport.
- Validation: targeted LUAD gallery tests, Ruff, and a clean Sphinx HTML build
  passed.

## 2026-07-30 - Documented contributor setup and docs builds

- Expanded `README.md` with clone/setup instructions using `uv`, development
  verification commands, pre-commit setup, Sphinx build and local preview
  commands, optional Playwright thumbnail rendering, and schema regeneration.
- Documented that the Sphinx build imports examples and generates the gallery
  pages/specs, so contributors have a complete path from clone to docs review.
- Validation: `uv run ruff check .`, `uv run sphinx-build -b html -W --keep-going
  docs docs/_build/html`, and `git diff --check` passed.

## 2026-07-30 - Upgraded the GenomeSpy core schema to 0.81.0

- Verified npm's current `@genome-spy/core` release and updated
  `[tool.genome-spy].core-version` from `0.80.0` to `0.81.0`.
- Regenerated the committed schema JSON, Python wrappers, typing/keyword
  helpers, channels, mixins, and schema exports from the pinned npm package.
- Updated generated-schema expectations for the new `arrow` mark and
  `SCHEMA_VERSION`; refreshed current codegen planning references to 0.81.0.
- Validation: 104 non-gallery tests, 23 targeted gallery tests, Ruff, mypy, and
  a strict Sphinx HTML build passed. The complete gallery test module was not
  rerun after the final schema assertion change, but its example/spec coverage
  and the full Sphinx gallery build passed.

## 2026-07-30 - Require real gallery thumbnails

- Removed the generated SVG poster fallback from the gallery collector and
  Sphinx extension; every example now requires a PNG thumbnail.
- Simplified the README and CI workflow so thumbnail rendering is no longer
  presented as an optional local setup or tolerated as a best-effort failure.
- Updated gallery tests to assert the missing-thumbnail error while keeping
  index tests independent of the local asset directory.
- Validation: focused thumbnail tests and full Ruff passed. A strict docs build
  is intentionally blocked until the manually reviewed
  `stacked_genome_browser.png` thumbnail is added.

## 2026-07-30 - Separated README contributor workflows

- Split the README's combined examples/schema section into separate
  `Work on examples` and `Regenerate schema wrappers` headings because they
  have different audiences and maintenance workflows.

## 2026-07-30 - Matched upstream thumbnail capture policy

- Compared the upstream `captureScreenshots.mjs` workflow with the Python
  gallery tooling. The upstream batch mode skips existing PNGs, while explicit
  selections recapture by default and `--overwrite` forces a full refresh.
- Updated `tools/render_thumbnails.py` with the same selection and overwrite
  behavior. The Sphinx extension remains the source of generated JSON specs;
  thumbnails continue to require real PNG files with no SVG fallback.
- Added focused tests for default and named example selection.
- Validation: focused renderer tests and Ruff passed.

## 2026-07-30 - Track gallery PNGs and remove QQ/admixture examples

- Removed the obsolete QQ-plot and admixture example modules and their gallery
  thumbnails.
- Stopped ignoring `docs/_static/gallery/` so the remaining real PNG thumbnails
  are available for Git tracking and release with the documentation.
- Removed the unused population-structure category and updated the embed-width
  test to use the Manhattan example.
- Validation: 36 gallery/thumbnail tests, Ruff, and a strict Sphinx HTML build
  passed.

## 2026-07-30 - Added first upstream grammar example slice

- Inventoried 86 documentation specs in the local GenomeSpy reference checkout
  and recorded a staged porting plan for reusable grammar, transform, genomic,
  and advanced examples.
- Added seven Python gallery examples for point, rect heatmap, ranged rule,
  link, sequence logo, vertical concatenation, and scrollable viewport patterns.
- Added deterministic grammar datasets and extended `transform_stack` with the
  upstream `baseField` option needed by sequence logos.
- Captured seven real PNG thumbnails with Playwright; the gallery now has 26
  PNGs and no SVG assets.
- Validation: browser capture, 43 gallery/thumbnail tests, Ruff, mypy, and a
  strict Sphinx HTML build passed.

## 2026-07-30 - Prepared for upstream genomic example refresh

- Reverted the two local-only changes in `tmp/genome-spy`: the modified lock
  file and the untracked sashimi debug test. The reference worktree is clean
  and ready for an upstream pull.
- Enabled zoom on the relevant scales in all seven newly added Basics
  examples, including both axes where the grammar example supports it.
- Validation: 39 gallery tests and Ruff passed.

## 2026-07-30 - Ported new genomic-data examples

- Reviewed the refreshed upstream `docs/genomic-data/examples` inventory and
  separated new examples from patterns already present in this gallery.
- Added Python examples for multiple sequence alignment, dynseq SPI1
  reference/alternate tracks, and TCGA OV GISTIC copy-number scores.
- Captured their real PNG thumbnails and documented the remaining advanced
  examples that need lookup, regex-fold, window, template-import, or richer
  parameter APIs before they can be ported cleanly.
- Validation: browser capture, 42 gallery tests, mypy, Ruff, and a strict
  Sphinx HTML build passed.

## 2026-07-30 - Audited upstream genomic API gaps

- Compared all 14 refreshed upstream genomic example specs against the current
  handwritten API and existing gallery coverage.
- Identified seven partial or blocked patterns: multiscale, coordinate lookup,
  regex fold, window/lead, view imports/templates, VCF self-lookup, cross
  joins, and richer parameterized interactions.
- Added a staged G1-G4 extension plan to `plans/genomic_api_plan.md`, ordered
  by reusable transform and composition helpers rather than individual plots.

## 2026-07-30 - Generated complete transform authoring coverage

- Extended schema generation to emit an Altair-style `TransformMethodMixin`
  from every member of the upstream `TransformParams` union. All 27 transforms
  now have public fluent methods, including Python-safe `as_` and `from_`
  parameters.
- Removed the duplicated handwritten transform implementations. The generated
  filter method preserves the positional expression convenience while also
  exposing the complete expression/selection filter schema.
- Added explicit `--package-dir` and `--schema-path` generation modes for
  auditing built local GenomeSpy changes without changing the pinned npm
  release workflow.
- Added and packaged `capabilities.json`, which records definitions, marks,
  channels, transforms, and root-spec variants. Tests now require every
  transform union member to be generated and reachable from `Chart`.
- Validation: 152 tests, Ruff, and mypy passed.

## 2026-07-30 - Reviewed codegen and completed root-spec loading

- Critically reviewed the generated transform work before continuing. Fixed an
  invalid-state bug where `transform_filter()` accepted neither an expression
  nor a selection parameter.
- Found and fixed a deeper generator omission: JSON Schema properties that are
  Python keywords were absent from generated constructors. Generated wrappers
  now expose Python-safe names such as `import_`, `from_`, `as_`, and `class_`
  while preserving the original JSON property names.
- Added `TopLevelSpec.from_dict()` and `from_json()` with root validation,
  structural dispatch, schema-URL preservation, deep-copy semantics, and exact
  round-tripping.
- Added public `MultiscaleChart`, `ImportedView`, `multiscale()`, and
  `import_view()` APIs. Imports remain child-only while multiscale views retain
  full rendering and composition behavior.
- Round-tripped all 13 refreshed upstream genomic examples supported by the
  pinned `0.81.0` schema. The unreleased ASCAT algorithm remains correctly
  rejected because its `cross` transform is absent from the published schema.
- Validation: 167 tests, Ruff, mypy, deterministic regeneration, and wheel
  packaging passed.

## 2026-07-30 - Reimplemented DynSeq with sequence logos

- Replaced the simplified DynSeq BigWig bar charts with the upstream
  architecture: indexed FASTA sequence loading, reusable reference/alternate
  view imports, dynamic BigWig coordinate lookup, and text marks using
  `logoLetters`.
- Extended public authoring support for multi-field tooltips, chart-valued
  templates, expression-backed lazy data URLs, and the generated `Tooltip`
  schema wrapper.
- Added focused assertions for the FASTA, coordinate lookup, imported views,
  and sequence-logo encodings, then captured a real browser thumbnail showing
  both allele logos.
- Validation: 170 tests, Ruff, mypy, strict Sphinx HTML build, browser capture,
  and `git diff --check` passed.

## 2026-07-30 - Removed implicit y-scale reversal

- Removed the handwritten authoring rule that silently added
  `scale.reverse=true` to every y encoding. GenomeSpy's default is now
  preserved, and reverse is serialized only when explicitly requested.
- Updated chart serialization tests to cover untouched scales and explicit
  reversal.
- Validation: 170 tests, Ruff, mypy, and `git diff --check` passed.

## 2026-07-30 - Recreated the complete GISTIC example

- Replaced the single-track TCGA OV GISTIC approximation with the complete
  upstream composition: signed q-value rectangles, zero and threshold rules,
  and the lower amplification/deletion lesion track.
- Expressed the lesion parsing directly with the generated `regexExtract`,
  `filter`, `regexFold`, and `project` transform methods.
- Added structural gallery coverage and captured a browser thumbnail that
  matches the upstream score and region/peak layout.
- Confirmed that `schema/capabilities.json` is a generated codegen audit
  manifest, not runtime chart configuration. It records discovered schema
  definitions, marks, channels, transforms, and root variants for coverage
  tests.
- Validation: 171 tests, Ruff, mypy, strict Sphinx HTML build, browser capture,
  and `git diff --check` passed.

## 2026-07-30 - Shared the stacked browser genomic axis

- Configured the stacked genome browser's x scale and x axis as shared, leaving
  one genomic axis below the complete track stack.
- Rewrote layer composition with `+` and vertical track composition with `&`,
  matching Altair's operator notation.
- Recorded `+`, `&`, and `|` as the preferred example composition style in
  `AGENTS.md` and removed redundant `reverse=False` signal-scale options.
- Validation: browser capture, 171 tests, Ruff, mypy, and `git diff --check`
  passed.

## 2026-07-30 - Recreated the full BAM alignment example

- Added Altair-style `+`, `&`, and `|` operators to imported views so imported
  tracks compose through the same notation as ordinary charts.
- Updated the stacked browser to use a root-owned shared genomic axis and
  Altair-style composition throughout.
- Replaced the simplified BAM example with the upstream dataflow: dynamic BAM
  loading, map-quality and base-quality controls, pileup, coverage, mismatch
  summaries, directional read arrows, CIGAR overlays, mismatch labels, and the
  zoom message.
- Exported the generated `Direction` channel through the public API. Preserved
  explicit nested layer boundaries where inherited encodings must not reach
  sibling views.
- Validation: browser capture, 173 tests, Ruff, mypy, strict Sphinx HTML build,
  and `git diff --check` passed.

## 2026-07-27 - Added the first stacked genome browser example

- Added `docs/examples/stacked_genome_browser.py`, a five-track hg38 browser
  view over `chr7:55,100,000-55,120,000`.
- The stack combines lazy GC-content and phyloP100way BigWig tracks, an ENCODE
  cCRE BigBed track, an indexed FASTA sequence track, and the existing packed
  RefSeq annotation pattern.
- All tracks use one root genomic x-domain, with independent y scales and
  y-axis labels so the example demonstrates composition rather than data
  preparation.
- Narrowed the initial locus and raised the FASTA fetch window after checking
  the lazy-source thresholds: this keeps cCRE, the EGFR label, and sequence
  content visible on first render without fetching a 100 kb base-resolution
  region.
- Matched the standalone cCRE and FASTA examples by removing synthetic
  constant y encodings from those tracks; their track titles provide the
  labels while the data remains driven only by the genomic x channels.
- Verified the five remote resources respond with HTTP byte-range support and
  confirmed the authored spec through the gallery regression test and Sphinx.

## 2026-07-27 - Debugged stacked-track visibility

- Confirmed the RefSeq and cCRE resources contain records in the selected hg38
  window and centered the initial domain on the EGFR label plus nearby cCREs.
- Fixed the indexed FASTA lazy-fetch boundary by using a 30 kb fetch window for
  the 20 kb visible domain; removed synthetic y encodings from the cCRE and
  sequence tracks to match their working standalone examples.
- Rebuilt the generated gallery spec and removed the explicit shared-x resolver
  to match the known-working `genome_tracks.py` composition pattern.
- Validation: 22 targeted gallery tests, Ruff, `git diff --check`, and a clean
  Sphinx HTML build passed. Live browser inspection was unavailable in this
  session and remains the follow-up if hosted rendering still omits tracks.

## 2026-07-26 - Aligned Manhattan annotations to HapMap hg18

- Corrected the HapMap Manhattan example from hg38 to hg18 after verifying that
  the source coordinates use the 2006 NCBI freeze, Build 36.1.
- Added a separate packed RefSeq transcript/label track from UCSC's hg18
  `refGene.txt.gz`, parsed with explicit headerless TSV columns.
- Composed the association and annotation tracks with a shared locus x scale
  and independent y scales.
- Added a spec regression test for the assembly, source format, and composition.
- Filtered the UCSC RefSeq source to primary hg18 contigs before linearization;
  the table also contains alternate haplotype contigs that are not present in
  GenomeSpy's built-in assembly definition.
- Moved the canonical hg18 contig order and headerless RefSeq source schema
  into the HapMap dataset helper so the user-facing example contains no
  assembly or source-column plumbing. HapMap chromosomes are normalized to
  UCSC chr1 through chrX names at the same boundary.
- Added the same semantic label zoom used by the RefSeq example: measured text,
  `filterScoredLabels`, and zoom-dependent label opacity keep gene names from
  overlapping at genome-wide scale.
- Mirrored the full RefSeq visual structure with exon rectangles, transcript
  bodies, strand-aware arrows, shared color encoding, and the new fluent
  `transform_flatten_delimited(...)` helper.
- Replaced the lexicographically ordered UCSC chromosome-size URL with an
  explicit hg18 contig order so the x-axis runs chr1 through chr22, then X/Y.
- Matched `flattenDelimited`'s parallel-array contract by providing one comma
  separator for each exon field; the follow-on metrics error was only a
  cascade from that initialization failure.
- Widened the annotation semantic-zoom thresholds for the whole-genome
  Manhattan viewport. The previous 100 kb to 40 kb thresholds made all
  annotation layers fully transparent at genome-wide scale.
- Kept transcript bodies visible at overview scale while restoring tighter
  semantic zoom for labels and strand arrows, so annotation names become
  readable only after zooming into a chromosome region.
- Restored the working RefSeq composition pattern: opacity belongs on the
  layered transcript group and strand-arrow view, while labels rely on
  filterScoredLabels for zoom-aware selection.
- Validation: gallery tests, Ruff, and the hg18 RefSeq range request passed.

## 2026-07-26 - Removed the Manhattan annotation experiment

- Removed the unstable hg18 RefSeq annotation branch from the Manhattan
  example at the user's request.
- Kept the example focused on the association plot until a reliable,
  preprocessed hg18 gene resource is available.

## 2026-07-26 - Added opt-in gallery dataframe previews

- Extended the gallery collector with an optional `DATA_PREVIEW` convention for
  one DataFrame or a named mapping of DataFrames.
- Captured only five rows and serializable string values, then rendered escaped
  HTML tables above the generated example source with responsive overflow.
- Enabled previews for the feasible tabular examples, including both
  oncoprints, association plots, airway expression plots, lollipop, rainfall,
  and admixture data, so readers can inspect input structure without notebook
  conversion or commented code.
- Validation: `31` gallery tests, Ruff, and a clean Sphinx HTML build passed.

## 2026-07-26 - Moved HapMap volcano preparation into datasets

- Added `hapmap_volcano_data(...)` to build the filtered, classified HapMap
  table and explicit volcano domains/cutoff values.
- Simplified `docs/examples/volcano_plot.py` so it focuses on GenomeSpy marks,
  encodings, scales, and composition rather than pandas/NumPy preparation.
- Added dataset regression coverage for the prepared columns and domains.
- Validation: `42` dataset and gallery tests plus Ruff passed.

## 2026-07-26 - Moved oncoprint preparation into datasets

- Added chart-ready `luad_oncoprint_data()` and `laml_oncoplot_data()` helpers
  with typed result objects in `src/genome_spy/datasets/_oncoprint.py`.
- Removed raw payload loading, repeated DataFrame copies, sample compaction,
  categorical ordering, and count-grid preparation from both oncoprint docs
  examples.
- Kept the examples focused on GenomeSpy composition, marks, encodings, and
  scales, while preserving the existing LUAD matrix and aligned tracks.
- Validation: `31` dataset/gallery tests and Ruff passed.

## 2026-07-26 - Pinned the working gallery embed path

- Confirmed that all gallery plots, including sashimi, render with a static
  import of the pinned GenomeSpy bundle and direct
  `embed(c, spec, { bare: true })` into the chart container.
- Documented this as a repository constraint in `AGENTS.md`; do not reintroduce
  canvas transform/resize wrappers into the gallery embed.

## 2026-07-26 - Moved association-plot preparation into datasets

- Added dataset-layer builders for airway differential expression and HapMap
  Manhattan/QQ data, including statistical results, transformed fields,
  classifications, ranks, thresholds, and plotting domains.
- Simplified the four pilot examples so they load chart-ready tables and focus
  on GenomeSpy marks, encodings, scales, and composition instead of importing
  SciPy, statsmodels, or performing pandas/NumPy wrangling.
- Added dataset contract tests for the derived columns and metadata; updated
  plot descriptions to omit implementation details.
- Validated with 12 dataset tests, 29 gallery tests, Ruff, mypy, and the Sphinx
  build. Follow-up: apply the same boundary to the remaining examples.

## 2026-07-26 - Fixed sashimi BigWig URL redirect

- Removed an accidental double slash from the sashimi BigWig URL. The old URL
  returned a redirect while the normalized URL returns the binary resource
  directly, which is safer for GenomeSpy's lazy genomic data loader.
- Added a gallery regression test for direct BigWig and BED resource URLs.
- Removed an invalid container-level encoding from the layered splice-junction
  view; the child arc and label layers already own the required encodings,
  matching the upstream sashimi grammar.
- Hardened live-embed sizing against lazy charts reporting zero canvas dimensions
  during initialization; the chart container is now observed until a real size
  is available instead of collapsing the host.

## 2026-07-26 - Set the dataset preparation boundary for docs examples

- Documented in `AGENTS.md` that examples must focus on the GenomeSpy
  visualization API, while statistical inference and derived plotting data
  belong in `src/genome_spy/datasets/` or curated fixture files.
- Extended `plans/example_cleanup_plan.md` with a dataset-preparation boundary
  covering p-values, multiple-testing correction, transformed values, rankings,
  classifications, and plotting domains.
- Confirmed that the current `Chart` API already accepts DataFrames directly;
  the planned work is to prepare chart-ready tables before examples load them,
  not to add another DataFrame abstraction.
- Follow-up: move the airway paired-test results and QQ/Manhattan derived fields
  into dataset builders or packaged fixtures, then simplify those examples.

## 2026-07-26 - Documented LUAD legend and tooltip rendering state

- Removed the LUAD mutation encoding legends and the separate hand-built
  `Matrix symbols` key so the gene matrix, side summary, and lower tracks remain
  aligned.
- Restored the compact transform-based documentation embed rendering at the
  user's request after natural-size scrolling and browser zoom introduced worse
  layout behavior.
- Confirmed the remaining tooltip issue is caused by transformed canvas pointer
  coordinates: GenomeSpy hit-testing uses unscaled canvas coordinates while the
  browser displays a scaled canvas.
- Validated the embed regression test, LUAD gallery test, Ruff, diff checks, and
  Sphinx builds. Follow-up: make pointer-coordinate conversion transform-aware
  if compact rendering and accurate tooltips are both required.

## 2026-07-26 - Corrected the LUAD oncoprint matrix axes

- Reworked the LUAD alteration matrix to use the same plot grammar as the LAML
  oncoplot: zoomable quantitative sample intervals on `x`/`x2` and an ordered
  categorical gene scale on `y`.
- Made the matrix grid itself own the categorical gene-label axis so row names
  remain visible at the large cohort width. Full-cell, half-cell,
  triangle, and star event layers now all resolve against the categorical gene
  scale; mutation rectangles use the same full-row shape and color carries the
  alteration distinction.
- Removed `axis=None` from the matrix mutation and symbol layers. GenomeSpy's
  shared layer-axis resolver treats one null member as suppressing the entire
  shared axis, which was why the grid's otherwise valid gene labels never
  appeared.
- Explicitly resolved the center composition's y axes as independent alongside
  its independent y scales, so the categorical matrix axis is not merged with
  the quantitative upper-track axes.
- Corrected the lower heatmap panel filters to match the payload's full group
  names, increased the embedded chart height for the restored tracks, and
  kept the alteration legend outside the continuous vertical track stack.
- Kept the center-column sample scale shared across burden, clinical, matrix,
  and lower heatmap tracks while making the center-column y scales explicitly
  independent, allowing the matrix's categorical gene axis to render beside
  its rows. The outer summary-column resolution remains explicitly
  independent.
- Fixed the thumbnail renderer's two-pass scaling so it measures the unscaled
  chart before fitting it to the card. Measuring an already transformed chart
  cropped the left-side matrix axis from wide LUAD thumbnails.
- Restricted every sample-aligned track to the 343 samples that have at least
  one alteration in the matrix, reducing the unused horizontal space while
  preserving the shared zoomable sample scale.
- Moved the genetic-alteration legend above the matrix as a compact horizontal
  legend and added titles to the top quantitative track axes. The legend was
  subsequently removed from this dense embedded plot so the matrix and its
  side summary remain aligned. Removed the separate hand-built matrix-symbol
  key as well; GenomeSpy currently places legends within the owning view rather
  than below an entire concatenated composition.
- Updated the live documentation embed to size its inner chart to the natural
  canvas width and allow scrolling instead of hiding overflow. Wide LUAD
  embeds are 1712px wide inside an 896px article column, so the old clipped
  shadow-root container made the right legend unreachable.
- Restored the original transform-based live-embed fitting at the user's
  request. This is the known version where tooltip coordinates remain visually
  imperfect, but it matches the earlier compact chart rendering.

## 2026-07-26 - Restored the minimal gallery embed

- Restored the last known working direct GenomeSpy embed path after the shared
  transform/resize wrapper caused all gallery charts to disappear.
- Kept the sashimi data URL and spec fixes separate from the gallery renderer.
- Validation: focused gallery embed test and a clean Sphinx HTML rebuild passed.
